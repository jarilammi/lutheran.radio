# Widget Functionality Roadmap

**Purpose**

This document is the authoritative living record of **WidgetKit home-screen widgets**, the **Control Center control widget**, and **ActivityKit Live Activity presentation** in Lutheran Radio — including cross-process state, App Intents, timeline refresh, and the snapshot-driven presentation model.

It serves as both the project backlog for remaining widget work and a self-contained reference for developers and coding agents. It complements (does not replace) the player **event** backlog in [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md) and the presentation contract in [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md).

**Target Architecture (Ultimate Goal)**

The finish line is a **hybrid, two-zone model** — not a migration from snapshots to cross-process events. WidgetKit and ActivityKit require frozen value-type snapshots; that constraint is permanent.

| Zone | Mechanism | Ultimate state |
|------|-----------|----------------|
| Main app | `SharedPlayerManager` actor + imperative snapshot saves + `PlayerEvent` | Hybrid: events are an **additive** in-process consumer path; authoritative mutations and snapshot writes remain primary |
| Extension + LA presentation | `PersistedWidgetState` → `SimpleEntry` / `ContentState` + `reloadTimelines` / LA push | **Snapshot-driven permanently**; extension never observes `PlayerEvent` |

**Cross-process (widgets, Control Center, Live Activity UI)**

- Frozen value-type snapshots are the permanent integration model. See [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md).
- Freshness stacks: session snapshot (`loadPersistedWidgetState` / `savePersistedWidgetState`), App Group optimistic instant-feedback keys, pending-action round-trips, and main-app-driven `WidgetRefreshManager.refreshIfNeeded` / `WidgetCenter.reloadTimelines`.
- Live Activity hot path uses in-memory pushes from `RadioLiveActivityManager`; widgets and Control widgets read persisted snapshots.

**Presentation**

- Three narrow, `Equatable` surfaces derived **once per snapshot**: `PlayerStatusPresentation`, `PlayerControlPresentation`, `WidgetNowPlayingDisplayModel`.
- Leaf views and LA regions consume explicit slices — not full `PlayerVisualState` policy re-derived in `body`.

**Commands & shims (permanent infrastructure)**

- Optimistic ``persistOptimisticWidgetSnapshot``, `refreshVisualStateFromPersistence`, liveness sentinel (`lastUpdateTime = 0`), and pending-action App Group keys are permanent cross-process surfaces. Main-app refresh consolidation does not remove them.

**What “done” means**

- **Required:** Tier 1 presentation hygiene + Tier 2 snapshot/intent test contracts (main-app test host + `WidgetSurface` coordinators).
- **Optional, late-stage:** Tier 3–4 main-app refresh dedup and media-surface coordination — observable widget/LA behavior unchanged; no architectural model change.

**Core Principles (Never Violate)**

- Progress must be **slow and piece-by-piece**. Every micro-step must be small, reviewable, and non-breaking.
- **Nothing is forced**: Imperative snapshot reads, direct `refreshIfNeeded` calls, Darwin notification round-trips, and widget optimistic writes remain primary. Event-driven refresh and additive observation run in parallel (see Event-Driven Refactor Roadmap).
- **Documentation standards & canonical references** (applies to every touched file, including this roadmap):
  - **Describe what ships today:** Comments and `///` documentation describe the system as it exists *now* — present tense, final architecture language. Source must not read like a plan in progress: no "phase", "step", "temporary", "will be", "TODO migrate", or migration scaffolding in production code.
  - **Link only to committed docs:** `SeeAlso:`, file headers, and cross-links cite documents that are **committed and tracked** on the branch being edited. Staged-but-unmerged drafts, untracked scratch files, agent prompts, and one-off analysis notes are not authoritative until they ship — do not point future readers at them. When uncertain whether a path is canonical, confirm with `git ls-files <path>` (empty output means do not cite).
- **Security invariants come first.** No changes may touch `Core/`, certificate validation, DNS TXT validation, or `SecurityModelValidator`. Widget extension code must not duplicate security logic.
- Every user-visible string uses `String(localized:)` / `NSLocalizedString` with table `"Localizable"` (all 32 languages). Widget gallery section name is `LutheranRadioWidget/InfoPlist.xcstrings` (`CFBundleDisplayName`), kept in lockstep with `"lutheran_radio_title"`.
- Every change must follow `CODING_AGENT.md` (structured documentation, `SeeAlso`, AGENT NOTE where appropriate, build gates).
- After every micro-step, this roadmap document must be updated to reflect completed work and adjust priorities.

**Relationship to Other Docs**

| Document | Role |
|----------|------|
| [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) | Permanent presentation contract (three narrow surfaces, pre-derivation, termination invariant) |
| [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md) | Player `PlayerEvent` emission + `WidgetRefreshManager` as Tier 2 consumer |
| [`docs/cold-launch-streamplay-regression-checklist.md`](cold-launch-streamplay-regression-checklist.md) | Regression guard for widget pause/play/switch SSOT (§6 stream switch, §7 widget persistence) |
| `WidgetSurface/` | Presentation-only embedded framework: intent coordinators, timeline factory, liveness policy, narrow display models, language chrome, pure Provider presentation assembly |
| Membership-exception `WidgetDisplayModels.swift` | ``WidgetProviderSnapshotResolver`` (SPM-coupled snapshot hygiene / catalog labels / assemble wrapper) |
| Membership-exception `WidgetIntentExecution.swift` | AppIntent `perform*` SSOT and side effects (mechanical split from `WidgetDisplayModels.swift`; requires ``SharedPlayerManager`` + ``WidgetRefreshManager``) |
| `SharedPlayerManager+NowPlaying.swift`, `StreamProgramMetadata.swift` | Now Playing metadata SSOT (`nowPlayingDisplayStrings`) shared with widget/LA formatters |
| `README.md` (SSOT section) | Index + cross-links |
| ``<doc:Architecture>`` (Core DocC) | Event-driven player architecture + cross-process widget reality |

---

**Current State (as of 2026-08-06)**

## Completed

### Presentation & Display Model

- **Three narrow presentation surfaces** established and documented: `PlayerStatusPresentation` (`makeStatusPresentation()`), `PlayerControlPresentation` (`makeControlPresentation()`), `WidgetNowPlayingDisplayModel` (`widgetNowPlayingDisplayModel(...)`). Status/control mappers live in `WidgetSurface/PlayerVisualState.swift`; metadata/emphasis types and resolver live in `WidgetSurface/WidgetNowPlayingDisplay.swift`. Provider assembly remains in `WidgetDisplayModels.swift`. See [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md).
- **P0 control migration complete (2026-06-27):** All play/pause glyph + tint sites in Small/Medium/Large widgets, Dynamic Island trailing/compactTrailing, Lock Screen, and Control widget consume `PlayerControlPresentation` exclusively. Remaining `isActivelyPlaying` / `buttonTintColor` reads are non-control (LIVE indicator, animation bars, decorative radio glyph) and are documented.
- **Widget Provider pre-derivation complete:** `SimpleEntry` carries `statusPresentation`, `controlPresentation`, and `widgetNowPlayingDisplayModel`, each computed once in `Provider.placeholder`, `snapshot`, `timeline` / `createEntry`. Family views read narrow fields from the entry; `WidgetMetadataRegion` receives only `WidgetNowPlayingDisplayModel`.
- **Live Activity outer-closure pre-derivation complete:** `dynamicIsland` closure hoists `statusPres`, `controlPres`, `metadataModel`, `isPlaying`, and `radioIconTint` once; expanded, compact, and minimal regions close over these values (no inline `makeStatusPresentation()` in region builders). `LockScreenLiveActivityView` derives status/metadata/control at the top of `body`.
- **Narrow family view inputs (2026-07-13):** `LutheranRadioWidgetEntryView` projects explicit slices into `SmallWidgetView` / `MediumWidgetView` / `LargeWidgetView` instead of passing full `SimpleEntry`. Reduces WidgetKit invalidation surface when unrelated entry fields change.
- **Control widget provider pre-derivation (2026-07-13):** `LutheranRadioWidgetControl.Value` carries pre-derived `statusPresentation` and `controlPresentation`; `Provider` derives once per read; toggle label consumes narrow fields only (symmetric with `SimpleEntry`).
- **Exhaustive `#Preview` matrix** in `LutheranRadioWidget.swift` exercises visual states × metadata presence/absence.
- **Canonical presentation reference doc** [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) (termination invariant, LA event-driven update model, contributor guidance). README SSOT subsection cross-links it.

### Cross-Process State & Intents

- **`PersistedWidgetState` in-process session snapshot** (memory-only visual policy, 2026-07-07): `loadPersistedWidgetState()` reads `inMemorySessionWidgetSnapshot` only; cold launch resets to factory `.prePlay` via `resetToFactoryDefaultsOnLaunch()`. Retired App Group visual/playback/language keys are purged only via ``clearPersistedVisualStateKeysFromDisk()`` (no disk restore). ``preferredWidgetLanguage()`` resolves snapshot → `bestInitialLanguageCode()` when widgets active → hard `"en"` (never bare App Group `currentLanguage`). Resolves event-roadmap **OI-1** for widgets. Protected by `testColdLaunchFactoryResetClearsDiskVisualStateAndReturnsPrePlay` + `testPreferredWidgetLanguageIgnoresRetiredBareCurrentLanguageKey`.
- **App Group + Darwin round-trip** for widget/Control/LA intents: extension writes optimistic snapshot + `pendingAction` (+ `pendingActionId`); main app processes via ``RadioPlayerCoordinator/checkForPendingWidgetActions()`` (single drain owner; ViewController / SceneDelegate are thin lifecycle shims) / `handleWidgetAction` / other coordinator SSOT paths. Widget pause uses `SharedPlayerManager.stop()` (no duplicated player logic). Widget switch mirrors `completeStreamSwitch` SSOT (**P5-1 shipped**).
- **Optimistic instant feedback** (`isInstantFeedback`, `instantFeedbackTime`, `instantFeedbackLanguage`) for sub-round-trip UI; 15 s validity window. Play/pause **proposal** (``languageForPlayPauseOptimisticWrite(resolutionLanguage:)`` / ``languageForLiveActivityOrWidgetOptimistic()``) uses ``settledLanguageForInstantFeedback()`` so leftover in-process session is not passed in when ``homeWidgetLiveChrome`` is strictly fresher. ``writeInstantFeedback(language:)`` still persists that freshness-settled code when the caller disagrees (empty extension RAM must not plant the device locale; leftover session language must not re-plant — same `updatedAt` vs session stamp as ``resolveHomeWidgetChromeFields``). ``loadSharedState()`` does not let a disagreeing instant-feedback language override that freshness-settled source (switch destination still flashes when it matches the fresher of session or chrome).
- **Optimistic snapshot write** ``persistOptimisticWidgetSnapshot(_:language:)`` — permanent widget/LA infrastructure for instant feedback; scoped to extension optimistic paths; `emit(_:)` guard suppresses `PlayerEvent` yields in the widget process.
- **Liveness heuristic SSOT:** `SharedPlayerManager.isMainAppProcessRecentlyActive()` (60 s window + `lastUpdateTime = 0` termination sentinel; App Group re-sync on read). Family views delegate the passive-branch decision to ``WidgetLivenessPresentation/shouldShowPassiveTapToOpen(isMainAppRecentlyActive:)`` (heartbeat remains in `SharedPlayerManager`). After ``hasCompletedProcessExitSessionTeardown()``, this process must not bump `lastUpdateTime` or restamp ``homeWidgetLiveChrome`` (overlapping ``sceneDidEnterBackground`` save). Protected by `testForceStaleLivenessMakesIsRecentlyActiveFalse_AndBumpRestores` and `testProcessExitTeardownLatchSuppressesLivenessBumpAndLiveChromeRestamp`. **Scope:** widget passive chrome + durable LA mirror distrust only — **not** main-app `play()` / cold-launch / resurrection (process-isolation policy; sticky intent is the play SSOT after factory reset).
- **WidgetSurface intent + entry SSOT (2026-07-14; intent execution file split 2026-07-23):** ``WidgetIntentCoordinators`` (toggle plans), membership-exception ``WidgetIntentExecution`` in `WidgetIntentExecution.swift` (cross-target side effects), ``WidgetTimelineEntryFactory`` (home/control blueprints). Extension `perform()` and Provider paths are thin delegates. Toggle-mapping tests in `WidgetIntentContractTests` call coordinators directly (no mirror helpers).
- **Pure presentation extraction into WidgetSurface (2026-07-18):** ``displayFlag(for:)`` and pure ``displayLanguageName(for:preferredStreamLanguage:)`` in `WidgetLanguageDisplay.swift`; ``WidgetProviderPresentationAssembly`` owns pure Provider slice assembly. Membership-exception ``WidgetProviderSnapshotResolver`` in `WidgetDisplayModels.swift` retains snapshot reads, actor hygiene, stream-catalog station labels, and a thin catalog-aware assemble wrapper. ``WidgetIntentExecution`` remains membership-exception in its own file (requires ``SharedPlayerManager`` + ``WidgetRefreshManager``).
- **WidgetSurface + extension-profile tests permanent-doc closeout (2026-07-15):** `CODING_AGENT.md` / `Agents.md` and `README.md` document the two-layer model (`WidgetSurface` + membership-exception SPM sources), default test-plan membership of `LutheranRadioWidgetTests` / `WidgetSurfaceTests`, and widget-only verification commands.
- **WML-1 (2026-06-11):** Pause retains parsed metadata in snapshot for subdued widget display; resume rehydrates when needed (**P5-12**).

### Refresh, Teardown & Live Activity

- **`WidgetRefreshManager`** debouncing/coalescing coordinator: privacy gate (`hasActiveLutheranWidgets`), teardown gate (`isSessionTeardownInProgress`), adaptive debounce, `.prePlay`→`.playing` coalesce, identical non-playing lastKnown skip, in-flight identical sticky-pause wake (``shouldCoalesceInFlightIdenticalPauseRefresh``), `immediate: true` for sticky pause/lock and `.cleared` (Connecting `.prePlay` is deferred so soft-resume playing can supersede). The first paint-epoch exception after ``liveChromeIdentitySkipWake`` still executes once.
- **Tier 2 event consumer** (Event-Driven Roadmap): internal `PlayerEvent` observer routes through identical `refreshIfNeeded` surface. Tests in `WidgetRefreshManagerEventTests.swift` (derivation matrix, event-path gates, debounce timing, soft-resume coalesce, privacy, immediate delivery).
- **Session teardown orchestration (2026-07-08):** `performSessionAndWidgetTeardown()`, `performPostStopWidgetHygiene()`, `forceStaleLivenessTimestampForTermination()`, post-teardown `reloadAllTimelines`. Protected by teardown + post-stop tests in `SharedPlayerManagerEventTests.swift`.
- **`RadioLiveActivityManager`** event-driven LA updates (no default fallback timer); `contentUpdates` observation via `WidgetEventObserver`; `lastPushedContent` dedup. Attribute-events subset protected in `RadioLiveActivityManagerTests.swift`.
- **MediaRemoteUI watchdog fix (2026-07-08):** async `teardownNowPlayingSession()` + teardown gate suppressing `reloadTimelines` during Now Playing detach.

### Widget Surfaces Inventory

| Surface | Entry point | State read | User actions |
|---------|-------------|------------|--------------|
| Small / Medium / Large home widgets | `Provider` (`AppIntentTimelineProvider`) | `refreshVisualStateFromPersistence` → `loadPersistedWidgetState` | `WidgetToggleRadioIntent`, `SwitchStreamIntent` |
| Control Center widget | `LutheranRadioWidgetControl` (`ControlWidgetToggle`) | `loadPersistedWidgetState` + liveness | Toggle via `ControlWidgetToggle` value provider |
| Live Activity (Lock Screen + DI) | `RadioLiveActivityManager` pushes `ContentState` | In-memory SPM + persisted fallback | `LiveActivityTogglePlaybackIntent`, `LiveActivitySwitchStreamIntent` |
| Widget configuration | `RadioWidgetConfiguration` (`WidgetConfigurationIntent`) | N/A | Stream picker for widget config |

### Documentation & Tests (Widget-Adjacent, Complete)

- **Event-driven consumer tests:** `WidgetRefreshManagerEventTests.swift` (20), `WidgetEventObserverTests.swift` (6), `RadioLiveActivityManagerTests.swift` attribute-events subset (6), widget-process guards in `SharedPlayerManagerEventTests.swift`.
- **Presentation mapper matrices (2026-07-14):** `PlayerPresentationMapperTests.swift` (9) — `makeStatusPresentation()` and `makeControlPresentation()` for all six `PlayerVisualState` cases; complements `WidgetDisplayModelsTests.swift` metadata resolver coverage.
- **Snapshot / lifecycle tests:** cold-launch factory reset, liveness sentinel, session teardown, post-stop hygiene, Now Playing metadata clear.
- **Now Playing metadata alignment (2026-06-25/26):** `updateNowPlayingInfo()` delegates to `StreamProgramMetadata.nowPlayingDisplayStrings(...)`; language-switch metadata clear triggers immediate Now Playing refresh. Same formatter family as widget/LA title resolution.
- **README + Architecture DocC** cross-link widget presentation surfaces and event-driven refresh.

---

## Architecture Status

### Snapshot-Driven Cross-Process Model

WidgetKit and ActivityKit receive **frozen value-type snapshots** across process boundaries. The widget extension process **cannot** observe `SharedPlayerManager.events` (main-app emission guard). Cross-process freshness therefore relies on:

1. **In-process session snapshot** (`PersistedWidgetState` via `loadPersistedWidgetState` / `savePersistedWidgetState`) — process-local visual/language (OI-1 memory-only for cold launch)
2. **Privacy-gated App Group live chrome** (``homeWidgetLiveChrome``) — visual + language + hasError projection for extension Providers when main-app session RAM is unreachable
3. **Privacy-gated App Group program-metadata mirror** (``homeWidgetStreamMetadata``) — program title/speaker peer (single concern; not folded into live chrome)
4. **Interactive paint wake tokens** (``homeWidgetInteractivePaintEpoch`` / signature + Darwin paint-advanced) — force LIVE entry body re-eval after optimistic/main settle; **not** visual SSOT; **not** root view `.id`
5. **App Group instant-feedback + pending-action keys** (optimistic language flash + one-shot commands; not primary visual SSOT once live chrome is stamped)
6. **Main-app-driven** `WidgetCenter.reloadTimelines` / `WidgetRefreshManager.refreshIfNeeded` — **wake signal only**; does not pass visual/language into WidgetKit (Providers re-read session + mirrors)
7. **Live Activity** in-memory pushes from `RadioLiveActivityManager` (hot path avoids disk; LA durable mirrors are a different privacy class — not home-gated)

This is **permanent architecture**, not a gap to close with events. Mechanism SSOT for live chrome: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md).

### Authoritative Writers & Readers

| Concern | SSOT | Writers | Readers |
|---------|------|---------|---------|
| Visual / language / hasError (session + cross-process) | In-process `PersistedWidgetState` + privacy-gated App Group ``homeWidgetLiveChrome`` | Main app ``savePersistedWidgetState`` / ``persistWidgetSnapshot`` → ``stampHomeWidgetLiveChromeFromSession`` (sticky pause, ``setPlaying``, switch hold, ``performActualSave``); gate-open re-stamp; widget ``persistOptimisticWidgetSnapshot`` / switch (optimistic) | ``WidgetProviderSnapshotResolver`` via pure ``resolveHomeWidgetChromeFields`` (**agreement → session; disagreement → fresher `updatedAt`; neither → factory**); Control widget same path |
| Stream metadata (session + cross-process) | In-process `PersistedWidgetState.streamMetadata` + privacy-gated App Group ``homeWidgetStreamMetadata`` | ``didUpdateStreamMetadata`` → ``persistStreamMetadataForWidgets()``; privacy-gate open re-stamp; language hygiene / gate close clear | ``WidgetProviderSnapshotResolver`` (session then mirror); LA via in-memory ContentState |
| Language (resolution helpers) | Session / live chrome / ``preferredWidgetLanguage()`` | Saves, widget switch intents, live-chrome stamp | Flag grids, metadata fallback strings (factory when session + chrome absent) |
| Permanent error | Session / live chrome `hasError` | Security / unrecoverable failure paths (projected into chrome) | Widget chrome, `deriveRefreshParameters` |
| Liveness | `lastUpdateTime` (+ sentinel `0`) | Saves, `bumpWidgetLivenessTimestamp`, termination (also clears live chrome) | `isMainAppProcessRecentlyActive`, passive branch — **owns interactive vs passive** (live chrome is not interactive proof) |
| Pending commands | `pendingAction` + `pendingActionId` + `pendingLanguage` | Widget/LA/Control intents | `getPendingAction`, main-app processors |

### Presentation Derivation Sites (Canonical)

| Surface | WidgetKit (`SimpleEntry`) | Control Center (`Value`) | ActivityKit (`ContentState` views) |
|---------|---------------------------|--------------------------|-------------------------------------|
| Status | Once in `Provider` → projected in `LutheranRadioWidgetEntryView` (interactive heal re-resolves LIVE) | Once in `Provider` → `Value.statusPresentation` | Once at top of `LockScreenLiveActivityView` and outer `dynamicIsland` closure |
| Control | Once in `Provider` → direction-bound home play/pause intents (``openAppWhenRun = false``) | Once in `Provider` → `Value.controlPresentation` | Once in outer `dynamicIsland` + `LockScreenLiveActivityView` |
| Metadata | Once in `Provider` → `entry.widgetNowPlayingDisplayModel` | N/A (not shown) | Once in outer `dynamicIsland` + `LockScreenLiveActivityView` |
| Language chrome (flag + name + alt-stream “current”) | Snapshot `currentLanguage` via Provider assembly / ``preferredWidgetLanguage()`` (home/Control; App Group + session hygiene) | N/A (not shown) | **`ContentState.currentLanguage`** pushed by ``RadioLiveActivityManager`` from main-app stream attach language; views hoist `context.state.currentLanguage` only |

Leaf views (`WidgetMetadataRegion`, play/pause buttons) must not re-derive canonical mapping rules. Home interactive LIVE re-resolves status/control via ``WidgetInteractivePaintHeal`` from suite wake tokens (paint epoch/signature + Darwin paint-advanced) as **body dependencies only** — never fold those tokens into a root EntryView `.id` (intent miss → host open). Mechanism: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §6.6.

### Non-Forcing Refresh Model

`WidgetRefreshManager.refreshIfNeeded` is invoked from **many** imperative call sites (SharedPlayerManager saves, AppDelegate, SceneDelegate, coordinator, ViewController, widget intents) **and** from the internal `PlayerEvent` observer. Duplicate triggers are harmless (debounce/coalesce/regress guards). Future consolidation is Tier 3 (late stage); see Event-Driven Roadmap Tier 4 inventory.

---

## Open Issues (Tracked Outside Tier Backlog)

### Force-quit liveness window (accepted residual; mitigations shipped)

**Status:** Documented platform limit + presentation mitigations

After abrupt force-quit (app-switcher card dismiss), `applicationWillTerminate` is often **not** delivered, so `lastUpdateTime` may remain non-zero for up to **60 seconds** and interactive chrome can briefly appear instead of `tap_to_open`. Observed-termination paths write sentinel `0` immediately; force-quit cannot. That short window is an accepted platform residual when no reboot identity change applies.

**Shipped presentation honesty (mechanism names):** extension ``bumpWidgetLivenessTimestamp`` does not reopen a full interactive session when main is not recently active or ``shouldDistrustDurableMirrorPlayPlanning()`` is true; reboot boot-identity mismatch distrusts residual live-chrome paint and residual-only home **play** planning; Control ``performControlWidgetToggle(isPlayingRequested:)`` refuses `true` under the same flags (pause still executes); factory reset / process-start clear residual system Now Playing before user-initiated main open may re-publish after attach.

**User-initiated main open vs residual reboot surprise:** passive home chrome after quit / reboot does **not** mean “radio is still live,” and it does **not** cancel open-app = radio when the human starts main. Product policy: after ``resetToFactoryDefaultsOnLaunch()``, special tuning then stream when sticky pause is absent **and** a scene is presentable — icon, Siri, and passive `widgetURL` opens. Background scene-create after jetsam waits for that presentable open. Residual App Group / OS media while the main process is not resident must not invent a multi-minute live session. See [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). Main-app `play()` is never gated by `lastUpdateTime == 0` (process isolation / OI-1). Physical-device re-confirm (2026-08-15): after the window, home is passive `tap_to_open`; user-initiated open auto-plays and live chrome tracks that session.

**SeeAlso:** `forceStaleLivenessTimestampForTermination()`, `isMainAppProcessRecentlyActive()`, ``shouldDistrustDurableMirrorPlayPlanning()``, ``resolveHomeWidgetChromeFields``, ``planHomeWidgetToggle``, ``WidgetIntentExecution/performControlWidgetToggle(isPlayingRequested:)``, ``clearSystemNowPlayingMetadataSynchronously()``.

### Widget extension cannot observe `PlayerEvent` (permanent)

**Status:** By design

Emission is guarded to the main app (`isRunningInWidgetProcess`). Extension processes rely on snapshot reads + `refreshVisualStateFromPersistence()` hygiene. Any future "consolidation" must preserve optimistic `persistOptimisticWidgetSnapshot` and provider read-refresh.

**SeeAlso:** Event-Driven Refactor Roadmap "Cross-process and extension reality".

### Live Activity ContentState acceptance under continuous lock (open residual)

**Status:** Open residual — thrash class closed; acceptance not guaranteed under lock

Write-side language SSOT, owned-language/visual suppress, deferred recreation eligibility, soft ensure, settled re-arm, dual-axis settle, sparse long-horizon re-arm, continuous-lock thrash smart-loosen (freeze soft budget + partial coarsen), delayed-re-read axis-heal, one-outstanding-visual coalesce, same-stream ineligible Connecting skip, ineligible language-only dest-language (preserve owned visual), ineligible stream-switch Connecting skip after dest language landed, ineligible freeze visual-differing playing skip, and post-quiet language-only long-horizon after freeze are **shipped**. Stall truth is ``contentUpdates`` **or** delayed re-read; **both** run ``applySystemContentUpdateHeal`` with prior ``lastSystemHeldContent``. Immediate post-await `content.state` is apply-in-flight and does not axis-heal. While ``inFlightContentPushCandidate`` is unconfirmed, visual-differing candidates coalesce (``shouldCoalesceVisualDifferingContentPushWhileInFlight``; latest wins, including pause) and flush once after committed observation; language-only same-visual still updates. Same-stream ineligible resume does not `Activity.update` Connecting over owned ``.userPaused`` / ``.playing`` (``shouldSuppressConnectingContentPushWhileIneligible``); stream-switch hold still Connecting when eligible. After dest language has landed on owned pause/playing while still ineligible, hold-active Connecting is also skipped. Ineligible dest-language over a committed pause/play glyph is language-only (``shouldPreserveOwnedVisualOnIneligibleLanguageMutation`` / ``ContentState/replacingCurrentLanguage(_:)``), including during stream-switch hold; language ensure 3/3 uses that candidate. After freeze, post-quiet language long-horizon keeps owned visual (``shouldKeepOwnedVisualOnPostQuietLanguageLongHorizon``); dual-axis long-horizon is skipped; dual-axis settle at ``setPlaying()`` stays. Connecting handshake does not consume recreation; ``startActivity()`` is request-eligibility gated; playing quiet does not starve post-clamp `.playing`. `Activity.request` visibility is start/recreate only. `Activity.update` apply is asynchronous — some ineligible visual mutations may still lag. Observed residual classes: multi-destination language stick after an early accept (no longer *our* Connecting overwrite of dest language); soft-resume visual freeze after churn (Apple dropping later `.playing`, not a Connecting overwrite we issued); occasional pause visual lag. Soft ensure / long-horizon **fire ≠ rendered flag**. Unlock / become-active owned-surface ensure remains the presentable safety rail, not a lock-screen paint guarantee. Home WRM honesty is independent and neither causes nor heals this residual. Do **not** re-open eligibility, write-side language SSOT, or home live-chrome paint as if those failed; do **not** re-stack short soft-ensure alone as the fix; do **not** end the only interactive LA solely for lag while request ineligible.

**SeeAlso:** [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md) (device-verified residual + thrash smart-loosen), ``RadioLiveActivityManager/ensureAuthoritativeContentOnForegroundIfNeeded()``, ``contentEnsureFreezeSoftBudgetExhausted``, ``armPostQuietLongHorizonDualAxisEnsureIfNeeded()``, ``shouldKeepOwnedVisualOnPostQuietLanguageLongHorizon(freezeSoftBudgetExhausted:playingQuietPending:isRequestEligible:)``, ``shouldCoalesceVisualDifferingContentPushWhileInFlight(inFlightVisual:candidateVisual:ownedVisual:languageOnlyPreservingOwnedVisual:)``, ``flushCoalescedContentPushIfNeeded(observed:)``, ``shouldSuppressConnectingContentPushWhileIneligible(isRequestEligible:ownedVisual:candidateVisual:)``, ``shouldPreserveOwnedVisualOnIneligibleLanguageMutation(isRequestEligible:destinationLanguage:ownedLanguage:ownedVisual:)``, ``shouldSuppressVisualDifferingPlayingContentPushWhileIneligible(isRequestEligible:freezeSoftBudgetExhausted:ownedVisual:candidateVisual:)``.

### Widget extension unit test coverage

**Status:** **Closed (2026-07-15)** — `WidgetSurface` framework, extension-profile tests, and permanent-doc closeout ship

Widget contract tests still run in `Lutheran RadioTests` (main-app host) via DEBUG seams. In addition:

| Target | Compile profile | Coverage |
|--------|-----------------|----------|
| `LutheranRadioWidgetTests` | **No** `LUTHERAN_MAIN_APP` (same SPM set as extension) | Coordinators (incl. LA multi-source resolve), factory, liveness, presentation mappers, metadata resolver, Provider assembly, optimistic intent / ``WidgetIntentExecution/perform*`` (home/Control/LA toggle + **LA stream switch**), durable LA toggle mirror, ``displayFlag`` / ``displayLanguageName``, refresh subset |
| `WidgetSurfaceTests` | Pure `WidgetSurface` framework | Swift Testing — coordinators (incl. LA resolve priority), liveness, factory, mappers |

AppIntent `perform()` bodies are thin delegates to ``WidgetIntentExecution/perform*``; those entry points compile and run under the extension profile in `LutheranRadioWidgetTests`. Both targets are in the default `Lutheran Radio.xctestplan`. Permanent agent docs (`CODING_AGENT.md` / `Agents.md`, `README.md`) describe the two-layer model (`WidgetSurface` + membership exceptions).

**SeeAlso:** ``WidgetIntentExecution``, ``WidgetIntentCoordinators``, `LutheranRadioWidgetTests/`, `WidgetSurfaceTests/`, docs/Widget-Presentation-Dataflow.md, CODING_AGENT.md (cross-target widget sources).

### Now Playing + Live Activity stacking (user education / strategy)

**Status:** Documented (2026-07-14); dual-card UX accepted

iOS renders **both** system Now Playing and Live Activity when both are active — expected behavior, not a bug. Program metadata is aligned via `StreamProgramMetadata.nowPlayingDisplayStrings(...)`. Stacking scenarios, LA start policy, metadata push cost, and QA screenshot matrix are canonical in [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). Coordinated refresh uses ``SharedPlayerManager/refreshAllMediaSurfaces(liveActivity:widgetRefresh:widgetRefreshImmediate:)``.

**SeeAlso:** `SharedPlayerManager+NowPlaying.swift`, `RadioLiveActivityManager.swift`, `StreamProgramMetadata.swift`.

### Lock-screen Live Activity toggle planning (fixed 2026-07-15; optimistic content 2026-07-18)

**Status:** Fixed

``LiveActivityTogglePlaybackIntent`` must not plan solely from extension-local ``SharedPlayerManager/currentVisualState``. With home-widget write suppression and a memory-only session snapshot, a cold extension defaults to `.prePlay` and inverted the first lock-screen pause into a redundant **play** (audio kept playing). Planning now prefers:

1. ActivityKit ``ContentState/visualState`` (same SSOT as the LA glyph), **except** system-held Connecting (``.prePlay``) when a control-definite peer (``.playing`` / ``.userPaused``) exists — stale Connecting freezes must not invert pause/resume
2. Durable App Group key ``liveActivityToggleVisualState`` (written on every LA content push **and** at sticky pause lock; **not** gated by `hasActiveWidgets`)
3. Actor / session-snapshot fallbacks

**SeeAlso:** ``WidgetIntentCoordinators/resolveLiveActivityToggleVisualState(liveActivityContent:durableMirror:actorVisualState:sessionSnapshot:)``, ``WidgetIntentCoordinators/planLiveActivityToggle(resolution:distrustDurableMirrorPlay:)``, ``PlayerVisualState/isDefinitiveMediaToggleVisual``, ``WidgetIntentExecution/performLiveActivityToggle()``, ``WidgetIntentExecution/executeOptimisticToggle(plan:language:)``, ``WidgetIntentExecution/pushOptimisticLiveActivityToggleContent(visualState:)``, ``ContentState/replacingVisualState(_:)``, ``RadioLiveActivityManager/recordOptimisticToggleContent(visualState:)``, ``SharedPlayerManager/persistLiveActivityToggleVisualStateMirror(_:)``, ``SharedPlayerManager/stop()``, ``SharedPlayerManager/shouldDistrustDurableMirrorPlayPlanning()``.

**Hardening (2026-07-15):** Factory reset explicitly clears the durable LA toggle mirror (not only LA-end / termination). After termination sentinel or device reboot (recorded boot identity mismatch), durable mirror alone must not plan **play** — ActivityKit `ContentState` remains trusted for real lock-screen glyphs (and for Connecting when no definitive peer exists).

**Optimistic ContentState (2026-07-18):** Toggle intents publish the post-toggle control visual into ActivityKit content (program metadata preserved) and align main-app ``lastPushedContent`` so a rapid second tap resolves from the post-toggle glyph rather than stale pre-tap content. The durable mirror remains the empty-activities fallback.

**Pause honesty while owned visual is still Connecting (2026-07-29):** ``stop()`` and home/Control ``executeOptimisticToggle`` warm the durable LA toggle mirror to ``.userPaused`` and publish optimistic ContentState (language preserved) without requiring prior owned ``.playing``. Multi-source resolve prefers definitive peers over frozen Connecting. See [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).

### Live Activity language chrome SSOT (closed 2026-07-19 — same class as LA toggle visual fix)

**Status:** Closed (2026-07-19)

**Shipped contract**

| Concern | SSOT | Writer | Reader |
|---------|------|--------|--------|
| LA language chrome (flag, name, alt “current”) | `ContentState.currentLanguage` | Main-app ``RadioLiveActivityManager`` on every start/update via ``SharedPlayerManager/liveActivityLanguageCodeForContentPush()`` (stream attach via ``mainAppLiveActivityLanguageCode()`` / ``selectedStream``, or destination language while ``streamSwitchConnectingLanguageCode`` is stamped — Connecting hold **or** sticky-paused stamp without hold — **not** privacy-gated ``preferredWidgetLanguage()`` under no-widgets) | `LockScreenLiveActivityView`, Dynamic Island language chrome — **only** `context.state.currentLanguage` (hoisted once) |
| Durable language mirror | App Group ``liveActivityCurrentLanguage`` | Same push sites as visual mirror; **not** gated by ``hasActiveWidgets``; cleared on LA end, termination, factory reset, privacy clear | ``languageForLiveActivityOrWidgetOptimistic()`` when ActivityKit activities / session snapshot are unavailable |
| Play/pause optimistic language | ``settledLanguageForInstantFeedback()`` (session vs ``homeWidgetLiveChrome`` by the same freshness rule as ``resolveHomeWidgetChromeFields``), then durable language mirror | ``performLiveActivityToggle()`` / ``executeOptimisticToggle(plan:language:)`` warm the language mirror from that resolve; ``handleWidgetPlay`` / ``handleWidgetStop`` use ``languageForLiveActivityOrWidgetOptimistic()``; home/Control play/pause use ``languageForPlayPauseOptimisticWrite(resolutionLanguage:)`` | Instant feedback — leftover session is not proposed when chrome is strictly fresher; never a lagging Live Activity ``ContentState`` language when session or live chrome already advanced; never bare ``preferredWidgetLanguage()`` alone when those hold the stream code |
| Stream-chip optimistic language | ContentState `currentLanguage` + durable language mirror | ``executeLiveActivityStreamSwitch`` / home switch → ``pushOptimisticLiveActivityStreamSwitchContent`` (destination language + Connecting or preserved pause; metadata cleared) **before** pending drain; main-app ``recordOptimisticStreamSwitchContent`` + ``ensureAuthoritativeLanguageContentIfNeeded()`` | Flag/name/alt-current advance at chip tap — never wait solely for main-app attach |
| Suppress vs owned language | Owned `Activity.content.state.currentLanguage` beats optimistic ``lastPushedContent`` | ``shouldSuppressLiveActivityContentPush``; post-update suppress memory from observed `content.state`; language ensure after media-surface / setPlaying / optimistic switch | Lock-screen flag tracks destination even when in-process suppress memory was advanced aspirationally |

**Why the defect existed (historical):** ContentState carried only visual + metadata; LA views re-derived language via ``preferredWidgetLanguage()``, which hard-defaults to `"en"` under memory-only session + no home widgets while Live Activity remains active. A later class: aspirational ``lastPushedContent`` (optimistic stream switch or pre-acceptance assignment) matched the destination candidate so ActivityKit IPC was suppressed while the system-held activity still showed the prior language.

**Non-goals preserved:** No home-widget write-suppression reopen; no security logic in WidgetSurface; Now Playing artwork policy unchanged; no invented `.playing` during stream-switch hold.

**ContentState convergence while request-ineligible:** Write-side language stamps, owned-language/visual suppress, soft language + playing ensure, post-hold settled language/playing soft-ensure re-arm + delayed post-settled ensure, **settled dual-axis acceptance** (prePlay → playing after stream attach), **ineligible language-only dest-language** (preserve owned pause/playing, including during stream-switch hold), **ineligible freeze visual-differing playing skip** (do not `Activity.update` `.playing` over a committed pause/play glyph while ineligible; play re-arm does not authorize that IPC), **post-quiet sparse long-horizon** single- and dual-axis re-arm (5s/15s/45s; after freeze, language long-horizon stays language-only), **continuous-lock thrash smart-loosen** (freeze soft budget + true-axis partial coarsen + gated nested post-settled), and **deferred** interactive recreation when request is ineligible are shipped. Soft language/playing ensure is bounded: after max attempts while ineligible, quiet pending stops status-driven thrash; language-only or visual-only repair defers while quiet; pause and opposite-axis mutations still push. Re-arm quiet on destination/play mutation, optimistic toggle / stream-switch, eligibility, become-active / foreground, `contentUpdates`, or sparse long-horizon. ``setPlaying`` sequences dual-axis settle (when owned is prePlay), settled language, settled playing, then soft ensure. Soft-ensure thrash protection collapses concurrent soft-push loops per axis, records deferred recreation pending **once** per freeze while ineligible, and rate-limits identical stall diagnostics. Thrash smart-loosen keeps quiet as cool-down after freeze soft-budget exhaust so same-stream partial stalls do not nest post-settled storms. While lock- or background-driven, system-held `content.state` may still trail destination candidates until ActivityKit applies an update (**fire ≠ rendered flag**; apply is async, not a start-only visibility ban); audio and the interactive card remain. End+request does **not** run while ineligible (surface continuity). On become-active / foreground with ownership non-nil, ``ensureAuthoritativeContentOnForegroundIfNeeded()`` clears quiet then runs dual-axis / soft language/playing ensure and may recreate only when request is eligible after soft ensure still fails (including after freeze soft-budget / dual-axis long-horizon exhaust). **Status:** thrash / nested self-rearm class **closed**; continuous-lock ContentState **acceptance residual still open** (multi-destination language stick, soft-resume visual freeze after churn, occasional pause visual lag). Unlock heal remains proven. Home WRM honesty is independent. Do not re-open eligibility, soft-resume playing ensure, write-side language-SSOT, or home live-chrome paint as if those failed; do not re-stack short soft-ensure alone. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md); open residual under [Open Issues](#live-activity-contentstate-acceptance-under-continuous-lock-open-residual).

**SeeAlso:** ``RadioLiveActivityManager/updateCurrentActivity()``, ``RadioLiveActivityManager/shouldSuppressLiveActivityContentPush(lastPushed:candidate:ownedContentLanguage:)``, ``RadioLiveActivityManager/ensureAuthoritativeLanguageContentIfNeeded()``, ``RadioLiveActivityManager/shouldPreserveOwnedVisualOnIneligibleLanguageMutation(isRequestEligible:destinationLanguage:ownedLanguage:ownedVisual:)``, ``RadioLiveActivityManager/shouldSuppressVisualDifferingPlayingContentPushWhileIneligible(isRequestEligible:freezeSoftBudgetExhausted:ownedVisual:candidateVisual:)``, ``RadioLiveActivityManager/pushSettledLanguageAcceptanceContentIfNeeded()``, ``RadioLiveActivityManager/ensureAuthoritativePlayingContentIfNeeded()``, ``RadioLiveActivityManager/rearmPlayingEnsureQuietPending()``, ``RadioLiveActivityManager/shouldRunLanguageContentEnsureSoftPushes(needsLanguageEnsure:destinationLanguage:quietPendingDestination:isRequestEligible:)``, ``RadioLiveActivityManager/shouldRunPlayingContentEnsureSoftPushes(needsPlayingEnsure:quietPending:isRequestEligible:)``, ``RadioLiveActivityManager/ensureAuthoritativeContentOnForegroundIfNeeded()``, ``RadioLiveActivityManager/recordOptimisticStreamSwitchContent(language:visualState:)``, ``SharedPlayerManager/liveActivityLanguageCodeForContentPush()``, ``SharedPlayerManager/mainAppLiveActivityLanguageCode()``, ``SharedPlayerManager/resetToPrePlayForNewStream(preserveActiveSleepTimer:connectingLanguageCode:)``, ``SharedPlayerManager/persistLiveActivityLanguageMirror(_:)``, ``SharedPlayerManager/languageForLiveActivityOrWidgetOptimistic()``, ``WidgetIntentExecution/performLiveActivityToggle()``, ``WidgetIntentExecution/pushOptimisticLiveActivityStreamSwitchContent(languageCode:visualState:)``, ``ContentState/replacingStreamSwitchDestination(language:visualState:clearStreamMetadata:)``, ``WidgetIntentCoordinators/optimisticLiveActivityVisualForStreamSwitch(from:)``, ``displayFlag(for:)``, [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md), [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md), [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md) (Live Activity language chrome on ContentState shipped; acceptance residual while lock-ineligible).

---

## Remaining Backlog (Prioritized for Slow, Safe Progress)

Ordered by increasing risk and decreasing isolation. Earlier items are safer.

### Tier 1 – Presentation Surface Hygiene (Low Risk, High Value)

**Goal:** Complete the narrow-input presentation migration (see `Widget-Presentation-Dataflow.md`). No behavior change except where an open SSOT gap requires a field on the presentation payload.

- [x] Introduce `PlayerControlPresentation` + `makeControlPresentation()` and migrate all control-glyph sites (2026-06-27).
- [x] Pre-derive `widgetNowPlayingDisplayModel` onto `SimpleEntry` in Provider (widgets).
- [x] Hoist metadata + control derivation to outer `dynamicIsland` closure.
- [x] **Narrow family view inputs (2026-07-13):** `SmallWidgetView` / `MediumWidgetView` / `LargeWidgetView` accept explicit slices (`statusPresentation`, `controlPresentation`, `metadataModel`, `currentStation`, `currentLanguageCode`, `availableStreams`); projected at `LutheranRadioWidgetEntryView`. No `visualState` on family views (semantic policy not required in bodies).
- [x] **LA expanded-region status dedup (2026-07-13):** `statusPres` hoisted in outer `dynamicIsland` closure; expanded `.center`, `.bottom`, and `minimal` close over it (no inline `makeStatusPresentation()` in region builders).
- [x] **Control widget provider pre-derivation (2026-07-13):** `LutheranRadioWidgetControl.Value` stores `statusPresentation` + `controlPresentation`; `Provider` derives once; toggle label reads narrow fields only (symmetric with `SimpleEntry`).
- [x] **Live Activity language on `ContentState` + chrome from `context.state` (2026-07-19):** `currentLanguage` on ``LutheranRadioLiveActivityAttributes.ContentState``; push from ``RadioLiveActivityManager`` via ``mainAppLiveActivityLanguageCode()``; Lock Screen / Dynamic Island language chrome read **only** `context.state.currentLanguage` (hoisted once). Language-only changes force ActivityKit update (`lastPushedContent` equality includes language).
- [x] **ActivityKit suppress uses owned content language (2026-07-28):** ``shouldSuppressLiveActivityContentPush`` never skips when candidate language differs from owned `content.state`; post-update suppress memory from observed state; ``ensureAuthoritativeLanguageContentIfNeeded()`` after stream switch / media-surface / setPlaying / optimistic switch.
- [x] **ActivityKit suppress uses owned content visual (2026-07-28):** ``shouldSuppressLiveActivityContentPush`` never skips when owned `content.state.visualState` differs from the candidate (optimistic last-pushed playing cannot block soft-resume / post-audible Connecting repair).
- [x] **Soft-resume / post-audible playing visual ensure (2026-07-28):** ``ensureAuthoritativePlayingContentIfNeeded()`` performs up to ``authoritativePlayingContentEnsureMaxAttempts`` soft pushes with post-update owned visual re-read; soft-resume publish no-op (actor already `.playing`) still reconciles LA visual. Pure visual freezes prefer soft retries over recreation.
- [x] **Live Activity recreation after stalled system-held chrome (2026-07-28):** After a bounded streak of `Activity.update` completions where system-held language or visual (pause **or** Connecting vs playing/pause) still mismatches the candidate, ``recreateInteractiveLiveActivityAfterStalledContent()`` ends and re-requests the interactive activity with destination language + current visual when request-eligible; playing ensure covers owned/last `.userPaused` / `.prePlay` after soft resume.
- [x] **Defer interactive LA recreation until request is eligible (2026-07-28):** End + `Activity.request` only when Live Activities are enabled **and** the application is active; when ineligible (lock/background visibility), keep the existing interactive surface and record pending ensure. Failed start re-binds system residual or sets pending; ``ensureInteractiveLiveActivityIfNeeded()`` on foreground/become-active restores the card under session policy. Concurrent updates suppressed during recreation. System-held ContentState may still converge after become-active (owned-surface soft ensure path below); that is orthogonal to this eligibility gate.
- [x] **LA play/pause optimistic language (2026-07-19):** ``languageForLiveActivityOrWidgetOptimistic()`` prefers session snapshot then durable language mirror before ``preferredWidgetLanguage()``; toggle path warms the language mirror from ContentState.
- [x] **Durable LA language App Group mirror (2026-07-19):** ``liveActivityCurrentLanguage`` — same lifecycle and non-`hasActiveWidgets` gating as ``liveActivityToggleVisualState``.
- [x] **Foreground language ensure with existing interactive activity (2026-07-29; owned-surface recovery rail hardened 2026-07-29):** When ownership is non-nil after become-active / foreground, ``ensureInteractiveLiveActivityIfNeeded()`` invokes ``ensureAuthoritativeContentOnForegroundIfNeeded()`` via ``shouldInvokeOwnedSurfaceForegroundEnsure`` — always consumes language/playing quiet and ``pendingInteractiveLiveActivityEnsure``; debounces dual SceneDelegate hooks / rapid resign-become thrash; re-invokes inside the window when chrome still lags and request is eligible. Soft path clears both quiet flags then bounded language + playing ensure; eligible-only recreation only if soft ensure still fails. **Does not** end the only LA while request is ineligible. Gates: `testForegroundOwnedSurfaceSoftEnsurePolicy`, `testOwnedSurfaceForegroundEnsureInvokeAndDebouncePolicy`, `testForegroundRecreationOnlyAfterSoftEnsureFailsAndRequestEligible`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Language ensure quiet pending while request ineligible (2026-07-29):** After ``authoritativeLanguageContentEnsureMaxAttempts`` without owned language acceptance while interactive request is ineligible, soft language ensure enters quiet pending for that destination; language-only status re-pushes defer; visual mutations still push. Re-arm on destination change (optimistic stream-switch shared by home-widget + LA chips), eligibility, become-active (clears quiet), or `contentUpdates`. Does **not** end while ineligible. Gates: `testLanguageEnsureQuietPendingAfterMaxAttemptsWhileIneligible`, `testLanguageOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Settled language acceptance after hold clear (2026-07-29; soft-ensure re-arm + delayed retries 2026-07-30):** ``pushSettledLanguageAcceptanceContentIfNeeded()`` after stream-switch hold clears (``setPlaying`` / soft-resume no-op) clears language quiet once, re-runs a **full** ``ensureAuthoritativeLanguageContentIfNeeded()`` soft budget for the destination, and while still lagging under ineligible request schedules bounded delayed post-settled soft ensure (``postSettledLanguageEnsureDelayedIntervalsMilliseconds``). Consume-once settle entry while ineligible; re-open on destination change, eligibility, become-active, or `contentUpdates`. Does **not** invent `.playing` during hold; does **not** end while ineligible. Soft path alone does not guarantee owned language acceptance for every lock-stretch playing switch — presentable-window heal is the proven recovery rail. Gates: `testSettledLanguageAcceptancePushAfterHoldClearWhileQuiet`, `testPostSettledLanguageEnsureRetriesScheduleWhileOwnedLanguageLags`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Settled playing acceptance after hold clear (2026-07-29; soft-ensure re-arm + delayed retries 2026-08-10):** ``pushSettledPlayingAcceptanceContentIfNeeded()`` clears playing quiet once after hold/connect clear when the actor is authoritative `.playing` and owned visual still lags (``.prePlay`` / ``.userPaused``), re-runs a **full** ``ensureAuthoritativePlayingContentIfNeeded()`` soft budget (not a single dual-axis push alone — a lone settle push that immediately re-armed quiet blocked ``setPlaying``’s trailing ensure), and while still lagging under ineligible request schedules bounded delayed post-settled playing soft ensure (``postSettledPlayingEnsureDelayedIntervalsMilliseconds``). Consume-once for the settle entry; re-open on optimistic toggle / stream-switch, eligibility, become-active, or `contentUpdates`. ``setPlaying`` / soft-resume sequence settled language then settled playing then soft playing ensure. Optimistic toggle language alignment prefers selected-stream / durable mirror over lagging ``lastPushedContent`` (``languageForOptimisticToggleContentAlignment``); main-app toggle IPC co-heals language when stream attach differs from owned ContentState. Does **not** invent `.playing` during hold/connect; does **not** end while ineligible. Gates: `testSettledPlayingAcceptancePushAfterHoldClearWhileQuiet`, `testPostSettledPlayingEnsureRetriesScheduleWhileOwnedVisualLags`, `testOptimisticToggleLanguageAlignmentPrefersSelectedStreamOverLastPushed`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Playing ensure quiet pending while request ineligible (2026-07-29):** After ``authoritativePlayingContentEnsureMaxAttempts`` without owned `.playing` acceptance while interactive request is ineligible, soft playing ensure enters quiet pending; visual-only `.playing` repair re-pushes defer; pause and language mutations still push. Re-arm on authoritative play mutation (``rearmPlayingEnsureQuietPending``), optimistic toggle / stream-switch, eligibility, become-active (clears quiet), or `contentUpdates`. ``setPlaying`` sequences settled language + settled playing acceptance then soft playing ensure. Does **not** invent `.playing` during hold; does **not** end while ineligible. Gates: `testPlayingEnsureQuietPendingAfterMaxAttemptsWhileIneligible`, `testPlayingOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Soft-ensure thrash collapse + deferred recreation announce-once (2026-07-29):** Concurrent language/playing soft-push re-entry collapses into one in-flight loop per axis; deferred recreation while ineligible records ``pendingInteractiveLiveActivityEnsure`` once per freeze (no deferred-log flood); identical candidate/owned stall diagnostics and quiet-skip ensure logs rate-limit once per signature/engagement. Imperative ``updateCurrentActivity`` on true mutations unchanged. Does **not** end while ineligible; does **not** invent `.playing`. Gate: `testSoftEnsureThrashCollapseAndDeferredRecreationAnnounceOnce`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Settled dual-axis acceptance + post-quiet long-horizon (2026-08-10):** ``pushSettledDualAxisAcceptanceContentIfNeeded()`` co-pushes destination language + playing when owned is ``.prePlay`` after attach; sparse long-horizon (5s/15s/45s) single- and dual-axis re-arms after quiet; dual-axis exhaust prefers eligible recreate on become-active. Soft-resume from ``.userPaused`` stays single-axis. Does **not** end while ineligible; does **not** claim full mid-lock acceptance. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Continuous-lock ensure thrash smart-loosen (2026-08-11):** Freeze soft budget after soft ensure exhaust while ineligible; partial re-arm only on true language-new / visual-new convergence; gate nested post-settled after soft-budget exhaust; eligible recreate preference after freeze soft-budget and/or dual-axis long-horizon exhaust. Thrash class closed; ContentState acceptance residual still open. Gate: `testContentEnsureFreezeThrashSmartLoosenPolicies`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Live Activity stall oracle + start eligibility + handshake + post-clamp playing (2026-08-21):** Immediate post-await `content.state` is apply-in-flight; ``shouldCommitStalledContentPushObservation`` waits for `contentUpdates` / delayed re-read. Connecting handshake does not consume recreation. ``startActivity()`` / `.startOrUpdate` consult ``isInteractiveLiveActivityRequestEligible``. Playing quiet does not starve post-clamp `.playing`. Gates: `testContentPushStallOracleWaitsForApplyConfirmation`, `testConnectingPlayingHandshakeDoesNotConsumeRecreationStreak`, `testInteractiveLiveActivityStartRequiresRequestEligibility`, `testPlayingOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Delayed re-read axis-heal + `contentUpdates` yield diagnostics (2026-08-21):** ``scheduleInFlightContentPushConfirmation`` captures prior ``lastSystemHeldContent`` then runs ``applySystemContentUpdateHeal`` after delayed re-read (same policy as `contentUpdates`). Immediate post-await does not axis-heal. Language-new delayed observation with visual still Connecting re-arms playing ensure and does not wipe stall bookkeeping. DEBUG ``handleActivityContentUpdate`` logs a rate-limited yield line (`systemLang`, `systemVisual`, activity id) so device recapture can distinguish observer silence from a heal miss. Does **not** invent `.playing`; does **not** end while ineligible. Gates: `testDelayedRereadObservationRunsAxisHealOnLanguageNewVisualLag`, `testContentUpdatesYieldDiagnosticsAreRateLimited`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **One outstanding visual mutation while apply is in-flight (2026-08-21):** ``shouldCoalesceVisualDifferingContentPushWhileInFlight`` skips a second visual-differing `Activity.update` while ``inFlightContentPushCandidate`` is unconfirmed (latest ``pendingCoalescedContentPushCandidate`` wins, including pause replacing playing-ensure). Language-only same visual as in-flight **and** owned still updates. ``flushCoalescedContentPushIfNeeded`` pushes once after delayed re-read, `contentUpdates`, or immediate post-await match. Duplicate unconfirmed playing pushes also coalesce. Soft-ensure attempt counters still bound the loop. Does **not** invent `.playing`; does **not** end while ineligible; does **not** claim lock-screen paint. Gate: `testVisualDifferingContentPushCoalescesWhileInFlight`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Same-stream ineligible resume does not spend an apply on Connecting (2026-08-21):** ``shouldSuppressConnectingContentPushWhileIneligible`` skips `Activity.update` Connecting (``.prePlay``) over owned ``.userPaused`` / ``.playing`` while request is ineligible. Stream-switch hold still publishes Connecting when request-eligible. Hold-active skip after dest language landed is the 2026-08-22 sibling. First start (owned already Connecting) and request-eligible still publish Connecting. Durable mirrors may still warm Connecting; owned lock-screen glyph is not overwritten. Ineligible dest-language over committed pause/playing is language-only (sibling). Does **not** invent `.playing`; does **not** claim lock-screen paint. Gate: `testSameStreamIneligibleResumeDoesNotPushConnectingOverPausedOrPlaying`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Post-quiet language long-horizon stays language-only after freeze (2026-08-21):** ``shouldKeepOwnedVisualOnPostQuietLanguageLongHorizon`` skips dual-axis long-horizon after freeze soft budget / playing quiet while request is ineligible. Language long-horizon candidate visual equals owned visual (``ContentState/replacingCurrentLanguage(_:)``; do not force `.playing`). Dual-axis settle at ``setPlaying()`` after hold clear stays. Playing long-horizon remains the visual rail. Stream-switch hold still Connecting. Does **not** invent `.playing`; does **not** end while ineligible; does **not** claim lock-screen paint. Gate: `testPostQuietLanguageLongHorizonStaysLanguageOnlyAfterFreeze`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Ineligible language mutation is language-only (2026-08-22):** ``shouldPreserveOwnedVisualOnIneligibleLanguageMutation`` keeps owned ``.userPaused`` / ``.playing`` via ``ContentState/replacingCurrentLanguage(_:)`` when request is ineligible and dest language ≠ owned language — including during stream-switch hold. Language ensure 3/3 uses the same candidate. Eligible switch, first start (owned already Connecting), and dest matching owned still publish Connecting. Same-stream ineligible Connecting skip unchanged. Does **not** invent `.playing`; does **not** end while ineligible; does **not** skip Connecting on an eligible switch. Gate: `testIneligibleLanguageMutationPreservesOwnedVisualInsteadOfConnecting`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Ineligible freeze must not spend visual-differing playing IPC (2026-08-22):** ``shouldSuppressVisualDifferingPlayingContentPushWhileIneligible`` skips `Activity.update` candidate `.playing` while request is ineligible after freeze soft-budget exhaust **or** over a committed ``.userPaused`` / ``.playing`` glyph. ``recordOptimisticToggleContent`` may still clear quiet for a later eligible cycle; it does not authorize this IPC. Language-only and pause honesty still update. Dual-axis settle after hold clear while owned is Connecting (freeze not exhausted) still may push `.playing`. Unlock-heal remains the presentable playing-glyph repair. Does **not** invent `.playing`; does **not** end while ineligible; does **not** add another ensure rail. Gate: `testIneligibleFreezeDoesNotPushVisualDifferingPlaying`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Same-stream warm cluster reuse on hard-resume (2026-08-23):** ``urlWithOptimalServer(for:allowSameStreamWarmReuse:)`` skips the EU/US ping pair for ``PlaybackAttachContext/resume`` while ``lastServerSelectionTime`` is inside ``sameStreamWarmServerReuseInterval``. Stream switch, cold launch, and recovery ``play()`` still ping after the 10 s throttle. Network-path reconnect nils the stamp. User pause still hard-tears Icecast (``isSoftPaused`` stays false). Does **not** skip Core pins or DNS TXT. Gate: `testWarmWindowReusesOnlyWhenSameStreamResumeIsAllowed`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Ineligible stream-switch Connecting after dest language landed (2026-08-22):** ``shouldSuppressConnectingContentPushWhileIneligible`` skips `Activity.update` Connecting (``.prePlay``) over owned ``.userPaused`` / ``.playing`` while request is ineligible, **including during stream-switch hold** after dest language has landed. Dest-lag remains language-only (``shouldPreserveOwnedVisualOnIneligibleLanguageMutation``); dest matching owned is this skip. Eligible switch still Connecting via ``isInteractiveLiveActivityRequestEligible``. First start (owned already Connecting) still Connecting. Dual-axis settle while ineligible with owned still Connecting (freeze not exhausted) stays for first start / owned already Connecting. Unlock-heal remains the presentable playing-glyph repair. Does **not** invent `.playing`; does **not** end while ineligible; does **not** skip Connecting on an eligible switch. Gate: `testIneligibleStreamSwitchDoesNotPushConnectingAfterDestLanguageLanded`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [ ] **Optional `WidgetDisplayProjection` bundle:** Single `Equatable` struct bundling the three presentation surfaces for explicit Provider → view handoff. Only if narrow-parameter migration proves noisy.

**Rule:** Presentation SSOT only for the language items; no home-widget privacy regression. No security surface changes.

### Tier 2 – Cross-Process Intent & Snapshot Contracts (Medium Risk)

**Goal:** Protect widget/LA/Control intent round-trips and optimistic snapshot semantics with fast unit tests (main-app host + DEBUG seams).

- [x] **`widgetNowPlayingDisplayModel` resolver matrix (2026-07-13):** `WidgetDisplayModelsTests.swift` — title/speaker/emphasis for every `PlayerVisualState` × metadata presence/absence × language fallback. Pure function; no WidgetCenter IPC.
- [x] **Pending-action dedup contract (2026-07-13):** `WidgetIntentContractTests` — rapid schedule replaces pending; `clearPendingAction(actionId:)` ignores stale IDs; `getPendingActionIfFresh` clears expired `pendingActionTime`.
- [x] **Optimistic `persistOptimisticWidgetSnapshot` contract (2026-07-13):** `WidgetIntentContractTests.testPersistOptimisticWidgetSnapshotWritesSnapshotWithoutPlayerEventYield` + authoritative overwrite + pending preservation; extends `testEmitSuppressesYieldWhenRunningInWidgetProcess`.
- [x] **Rename `forcePersistVisualState` → `persistOptimisticWidgetSnapshot` (2026-07-13; alias retired 2026-07-22):** Public API is ``persistOptimisticWidgetSnapshot(_:language:)`` only; deprecated `forcePersistVisualState` forwarding wrapper removed after zero remaining call sites; canonical docs and SSOT tables use the present-tense name.
- [x] **Widget switch SSOT regression (2026-07-13):** `WidgetIntentContractTests` — paused optimistic snapshot preserved on widget switch; `handleWidgetSwitchToLanguage` paused reconciliation + `processedActionIds` dedup (checklist §6.11 / §6.13).
- [x] **Instant-feedback expiry (2026-07-13):** `WidgetIntentContractTests` — `loadSharedState` prefers instant-feedback within 15 s when it agrees with session / live chrome (or those are empty), falls back after expiry; play/pause of a settled stream does not override with a disagreeing prior language.
- [x] **Instant-feedback write follows settled chrome:** ``writeInstantFeedback(language:)`` persists the first settled session / ``homeWidgetLiveChrome`` code when the caller language disagrees (`WidgetIntentContractTests` / `WidgetIntentContractExtensionTests` — `writeInstantFeedback("sv")` with chrome `et` stores `et`; ``loadSharedState()`` ignore remains).
- [x] **Play/pause pending-action drain (2026-07-14):** `WidgetIntentContractTests` — optimistic `signalWidgetPendingAction` (play/pause), main-app `checkForPendingWidgetActions` drain (play, pause, double-pause ignore, debounce), Darwin notify + foreground drain, UITestMode drain-only, home-widget and Control-widget toggle mapping matrices + end-to-end signal paths, `handleWidgetPauseAction` coordinator surface (13 tests; method names in Tier 5 play/pause index).
- [x] **Extension-hosted opposite-tap drain + mailbox (2026-07-18):** Same-direction-only 0.65 s debounce (opposite play↔pause always runs); drained play/pause share the media-transport mailbox; pause coordinator path is `async` with no nested Task hop. Unit gates: `testCheckForPendingWidgetActionsAllowsOppositePlayPauseWithinDebounceWindow`, `testCheckForPendingWidgetActionsAllowsOppositePausePlayWithinDebounceWindow` (same-direction debounce retained). Architecture: Live-Activity-Stacking-and-Media-Surfaces (extension-hosted path).
- [x] **Joined optimistic → drain → authoritative state (2026-07-17):** `WidgetIntentContractTests` — multi-phase contracts that join extension-shaped optimistic write + pending signal with main-app drain and authoritative visual/intent postconditions (play and pause); post-drain refresh gate observation without `WidgetCenter` IPC; UITestMode without pending-action bypass clears optimistic play pending without executing. Method names in Tier 5 joined-round-trip index.

**Rule:** Use UITestMode / DEBUG seams; never call real `WidgetCenter.reloadTimelines` or ActivityKit IPC in unit tests. Follow fast-test patterns in `CODING_AGENT.md`.

### Tier 3 – Refresh & Provider Orchestration (Medium–High Risk, Late Stage)

**Goal:** Reduce redundant work without altering observable widget behavior.

**Scope:** Consolidation here is **main-app `refreshIfNeeded` call-site dedup only** — not replacing the snapshot model, not streaming `PlayerEvent` across process boundaries, and not removing optimistic extension writes or provider read-refresh hygiene.

- [x] **Provider `refreshVisualStateFromPersistence` audit (2026-07-13; hop removed 2026-08):** Documented in **Provider snapshot audit** below and implemented via ``WidgetProviderSnapshotResolver`` in `WidgetDisplayModels.swift`. Home-widget and Control-widget Providers paint via ``resolveFromSnapshot()`` (session + privacy-gated ``homeWidgetLiveChrome``). ``resolveWithActorHygiene`` is removed. Control retains a narrow ``refreshVisualStateFromPersistence()`` only when the App Group suite is unavailable.
- [x] **Interactive home LIVE heal + in-widget pause/play (2026-08-09):** Entry always re-resolves via ``WidgetInteractivePaintHeal`` / suite wake tokens (``homeWidgetInteractivePaintEpoch`` / signature + Darwin ``homeWidgetInteractivePaintAdvanced``) as body deps only — **no** thrashy root EntryView `.id` (that caused intent-miss host open: default host open with no mailbox pause). Direction-bound ``WidgetPlayRadioIntent`` / ``WidgetPauseRadioIntent`` (``openAppWhenRun = false``); stable Button identity + expanded hit target. Eyes-on: background home pause → Darwin + pending ``pause`` → ``userPaused`` while main stays backgrounded; soft-resume / re-pause / paused switch round-trip. Canonical: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §6.6.
- [x] **Imperative `refreshIfNeeded` deduplication (main app only, 2026-07-13):** Removed duplicate imperative calls from ``performActualSave``, ``didUpdateStreamMetadata``, and ``RadioPlayerCoordinator/updateUserDefaultsLanguage``. Tier 2 observer is now the sole driver for mutation-path timeline reloads; ``refreshUsesImmediateDelivery(for:hasError:)`` extended for sticky-pause and error urgency parity. Retained imperative paths: lifecycle/teardown (`performSessionAndWidgetTeardown`, ``performPostStopWidgetHygiene``, termination), AppDelegate foreground, widget-extension optimistic intents, widget-process ``handleWidgetPlay``/``handleWidgetStop`` delayed refresh.
- [x] **Widget-action polling timer evaluation (2026-07-13):** Removed `ViewController.setupWidgetActionPolling()` (30 s repeating timer). Darwin notify + 1…5 s launch burst + `SceneDelegate` become-active/foreground hooks are sufficient; background timers are unreliable while suspended.
- [x] **Control widget value-provider freshness (2026-07-13):** ``LutheranRadioWidgetControl/Provider`` uses ``WidgetProviderSnapshotResolver/resolveFromSnapshot()`` (symmetric with the home-widget Provider). App Group–nil is the only Control path that still calls ``refreshVisualStateFromPersistence()``.

**Rule:** Nothing removed until event + imperative paths produce identical observable widget/LA behavior on device.

#### Provider snapshot audit (canonical)

| Provider path | Actor hop (`refreshVisualStateFromPersistence`) | Snapshot read | Notes |
|---------------|--------------------------------------------------|---------------|-------|
| Home widget `placeholder` | No | N/A (synthetic `.prePlay`) | Static preview only |
| Home widget `snapshot` / `timeline` / `createEntry` | **No** | ``resolveFromSnapshot()`` | Paint SSOT is session + ``homeWidgetLiveChrome``; never ``currentVisualState`` on the happy path |
| Control widget `previewValue` | No | N/A (synthetic `.prePlay`) | Static preview only |
| Control widget `currentValue` | **Only if App Group nil** | ``resolveFromSnapshot()`` primary | Narrow ``refreshVisualStateFromPersistence()`` then actor visual when suite unavailable |
| Widget AppIntent optimistic (`ToggleRadioIntent`, etc.) | No | ``loadPersistedWidgetState()`` before write | Followed by extension-local ``refreshIfNeeded(..., immediate: true)`` |

**Safe direct-read rule:** Home and Control Providers paint from ``resolveFromSnapshot()`` and do not use ``currentVisualState`` on the happy path. ``refreshVisualStateFromPersistence()`` remains for main-app cold launch and Control’s App Group–nil fallback only.

#### Dual-path `refreshIfNeeded` inventory (with ``WidgetRefreshTrigger``)

Every production call passes an explicit ``WidgetRefreshTrigger``. DEBUG dual-fire observation soft-logs event+imperative pairs within ~150 ms (`[WidgetRefreshDualTrigger]`); not a product failure. Canonical detail: [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md) Tier 4 §2.

| Trigger | Caller | Status | Role |
|---------|--------|--------|------|
| ``.playerEvent`` | ``handlePlayerEvent`` | **Sole mutation-path driver** | Tier 2 observer; urgency via ``refreshUsesImmediateDelivery`` |
| ``.lifecycle`` | `AppDelegate.applicationWillEnterForeground` | Retained | Foreground re-sync; no ``PlayerEvent`` |
| ``.teardown`` | ``performSessionAndWidgetTeardown`` / ``performPostStopWidgetHygiene`` / termination | Retained | Privacy/teardown + sticky post-stop; may dual-fire with stop emissions |
| ``.extensionOptimistic`` | ``WidgetIntentExecution`` optimistic toggle/switch; ``handleWidgetPlay``/``handleWidgetStop`` | Retained | Extension cannot emit; immediate optimistic reload |
| ``.mediaSurface`` | ``refreshAllMediaSurfaces(widgetRefresh: true)`` | Opt-in (default `false`) | Explicit NP/LA + widget coordination |
| — | ``performActualSave`` / ``didUpdateStreamMetadata`` / ``updateUserDefaultsLanguage`` | **Removed** (2026-07-13) | Observer owns mutation-path reloads |

### Tier 4 – Media Surface Coordination (Cross-Cutting)

**Goal:** Keep Now Playing, Live Activity, and widget metadata visually aligned without duplicating formatters.

- [x] **Shared metadata formatter:** `StreamProgramMetadata.nowPlayingDisplayStrings(...)` SSOT; `updateNowPlayingInfo()` delegates; language-switch metadata clear triggers immediate Now Playing update.
- [x] **Stacking & LA policy validation (2026-07-14):** [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md) — LA start policy, stacking matrix, metadata push cost (`lastPushedContent` dedup), manual QA screenshot scenarios.
- [x] **`refreshAllMediaSurfaces()` wrapper (2026-07-14):** ``SharedPlayerManager/refreshAllMediaSurfaces(liveActivity:widgetRefresh:widgetRefreshImmediate:)`` in `SharedPlayerManager+NowPlaying.swift`; consolidated `setPlaying` / `stop` / pause surfaces; fixed `markAsUserPaused` / `markPlaybackStoppedByStreamFailure` LA drift; removed redundant coordinator NP+LA duplicates.
- [x] **ICY StreamTitle single-owner path (2026-07-29):** Live ICY mutates only via ``DirectStreamingPlayer/safeOnMetadataChange`` → ``SharedPlayerManager/didUpdateStreamMetadata(_:)`` (idempotent on identical raw). Coordinator `onMetadataChange` is in-app ViewModel only (no dual SSOT re-entry). Catalog ``Stream/title`` is never fed as StreamTitle; language-code change clears prior ICY so station/language presentation fallbacks apply until real StreamTitle. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md) “ICY StreamTitle Single-Owner Path”. Gates: `testDidUpdateStreamMetadataIdenticalRawIsIdempotent`, `testDidUpdateStreamMetadataNilClearsCatalogShapedPollution`, `testCatalogShapedStationDashLanguageParsesLanguageAsProgramTitle`.

**Rule:** No security surface changes. Metadata formatters stay in shared non-Core types.

### Tier 5 – Documentation & Tests

**Goal:** Complete widget extension test coverage and make widget contracts discoverable alongside the event-driven roadmap.

- [x] [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) — canonical presentation reference.
- [x] README SSOT widget presentation subsection + Architecture DocC event-driven section (cross-links widgets).
- [x] `WidgetRefreshManager` consumer test suite (20 tests) + teardown/post-stop + liveness + cold-launch factory reset.
- [x] **This roadmap** — living document; update after each micro-step.
- [x] **Cross-link from Event-Driven Refactor Roadmap (2026-07-13):** Tier 3 completion entry + mutation-path dedup inventory cross-link added.
- [x] **README subsection (2026-07-14):** "Widget & Live Activity functionality" table (presentation, stacking doc, wrapper, test index).
- [x] **Presentation mapper test index (2026-07-14):** `PlayerPresentationMapperTests` — `testMakeStatusPresentationMatrixMapsEveryVisualState`, `testMakeStatusPresentationSystemImagePolicyPerVisualState`, `testMakeStatusPresentationProducesNonEmptyLocalizedTextForAllStates`, `testMakeStatusPresentationIsDistinctAcrossAllVisualStates`, `testMakeControlPresentationMatrixMapsEveryVisualState`, `testMakeControlPresentationUsesPauseGlyphOnlyWhenActivelyPlaying`, `testMakeControlPresentationTintMatchesButtonTintColorPolicy`, `testMakeControlPresentationPlayingDiffersFromAllNonPlayingStates`, `testMakeControlPresentationNonPlayingStatesRemainDistinctByTint`.
- [x] **Tier 2 test index (2026-07-13):** `WidgetDisplayModelsTests` (metadata resolver matrix + `WidgetProviderSnapshotResolver`); `WidgetIntentContractTests` (pending-action dedup, instant-feedback expiry, `persistOptimisticWidgetSnapshot`, widget switch SSOT).
- [x] **Tier 2 play/pause drain test index (2026-07-14):** `WidgetIntentContractTests` — `testSignalWidgetPendingActionPlayWritesOptimisticSnapshotAndPending`, `testSignalWidgetPendingActionPauseWritesOptimisticSnapshotAndPending`, `testCheckForPendingWidgetActionsDrainsPlayPending`, `testCheckForPendingWidgetActionsDrainsPausePending`, `testCheckForPendingWidgetActionsIgnoresPauseWhenAlreadyUserPaused`, `testCheckForPendingWidgetActionsDebouncesRapidPlayTaps`, `testNotifyMainAppThenForegroundDrainExecutesPlayPending`, `testUITestModeWithoutBypassDrainsPendingWithoutExecuting`, `testHomeWidgetToggleActionMappingMatrix`, `testControlWidgetToggleActionMappingMatrix`, `testSignalWidgetPendingActionUsesHomeWidgetToggleMapping`, `testSignalWidgetPendingActionUsesControlWidgetToggleMapping`, `testHandleWidgetPauseActionSetsUserPaused`.
- [x] **Joined optimistic→drain round-trip test index (2026-07-17):** `WidgetIntentContractTests` — `testOptimisticPlaySignalThenDrainEstablishesAuthoritativePlayingState`, `testOptimisticPauseSignalThenDrainEstablishesAuthoritativeUserPausedState`, `testOptimisticPlayDrainRequestsWidgetRefreshPassingGuards`, `testOptimisticPlaySignalWithoutPendingBypassClearsWithoutExecuting`.
- [x] **Provider synthesis test index (2026-07-14; hop-removal names 2026-08-14):** `WidgetDisplayModelsTests` — ``WidgetProviderSnapshotResolver/resolveFromSnapshot()`` + ``assemblePresentationSlices(from:)`` for `SimpleEntry` / Control-widget `Value` field assembly (`testProviderSnapshotResolverReturnsPersistedFields`, `testProviderSnapshotResolverDefaultsToPrePlayWhenSnapshotAbsent`, `testRefreshVisualStateFromPersistenceReloadsActorVisualStateFromSnapshot`, `testAssemblePresentationSlicesMatrixMapsEveryVisualState`, `testAssemblePresentationSlicesUsesConnectionErrorWhenHasError`, `testAssemblePresentationSlicesCarriesStreamMetadataIntoNowPlayingModel`, `testAssemblePresentationSlicesMatchesControlWidgetValueDerivation`, `testAssemblePresentationSlicesFromPersistedSnapshotMatchesProviderContract`). Extension-profile `WidgetDisplayModelsExtensionTests` covers the same resolve path plus live-chrome / paint-heal matrices. Shared assembly lives in `WidgetDisplayModels.swift`; home-widget and Control-widget Providers consume it. Deleted ``resolveWithActorHygiene`` tests are **not** current gates.
- [x] **Now Playing formatter + media-surface coordination test index (2026-07-14; ICY dash parse + parse→formatter chain 2026-07-21):** `StreamProgramMetadataTests` — ICY ``from(rawICYMetadata:)`` for ASCII `" - "`, spaced en dash (U+2013), spaced em dash (U+2014), multi-segment re-join, unspaced hyphen non-split, `" by "`, empty/`hasDisplayableContent`; `nowPlayingDisplayStrings` matrix (parsed title/speaker, raw fallback, station fallback, whitespace); raw-ICY → parse → formatter chains `testNowPlayingDisplayStringsFromEnDashRawICYYieldsSpeakerArtistLine`, `testNowPlayingDisplayStringsFromEmDashRawICYYieldsSpeakerArtistLine` (speaker on artist line); widget program-title alignment; `SharedPlayerManagerEventTests` — `testRefreshAllMediaSurfacesOrdersNowPlayingBeforeWidgetRefreshAndWritesDisplayStrings` (coordination order log + `MPNowPlayingInfoCenter` SSOT under DEBUG bypass; LA IPC skipped).
- [x] **Now Playing `playbackState` + remote-command hygiene (2026-07-18):** Live ``updateNowPlayingInfo()`` aligns dictionary rate and `MPNowPlayingInfoCenter.playbackState` (`.playing` / `.paused`); teardown retains `.stopped`. ``configureNowPlayingControlsIfNeeded()`` enables only play/pause/toggle/stop and disables unsupported remote commands. Gates: `testUpdateNowPlayingInfoSetsPausedPlaybackStateWhenNotActivelyPlaying`, `testConfigureNowPlayingControlsDisablesUnsupportedRemoteCommands` (+ extended NP SSOT assertions). Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Media-transport command serialization (2026-07-18):** ``MediaTransportCommand`` mailbox on ``SharedPlayerManager`` serializes Now Playing / headset play-pause-toggle-stop and main-app Live Activity engine execution (`submitMediaTransportCommand` / `AndWait`). Pause/stop preempt in-flight play; toggle samples actor state only after prior verbs commit. Gates: `testMediaTransportDoubleToggleWhilePlayingEndsPlaying`, `testMediaTransportDoubleToggleWhilePausedEndsPaused`, `testMediaTransportPausePreemptsInFlightPlayOnMailbox`, `testMediaTransportInterleavesLiveActivityPauseWithRemoteToggle`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- [x] **Media toggle planning for Connecting / thermal / security (2026-07-18):** ``isActivelyPlaying`` stays “audio flowing”; toggle planning uses ``plansMediaToggleAsPause`` / ``blocksPlannedPlay`` / ``optimisticVisualAfterPlayPlan`` plus ``isConnectingPlayback`` start pipeline (cancel connect, thermal refuse, security recovery Connecting chrome). Gates: WidgetSurface / extension coordinator matrices; `testMediaTransportToggleWhileConnectingCancelsToUserPaused`, `testUserRequestedPlayWhileConnectingIsIdempotent`. Canonical: Live-Activity-Stacking-and-Media-Surfaces.
- [x] **WidgetSurface coordinator SSOT (2026-07-14):** `WidgetIntentCoordinators`, `WidgetTimelineEntryFactory`, `WidgetLivenessPresentation`, `WidgetNowPlayingDisplay` in `WidgetSurface/`; ``WidgetIntentExecution`` in membership-exception `WidgetIntentExecution.swift` (split from `WidgetDisplayModels.swift`); extension thin delegates; `WidgetIntentContractTests` toggle matrices use ``planHomeWidgetToggle(from:)`` / ``planControlWidgetToggle(isPlayingRequested:)``.
- [x] **LiveActivitySwitchStreamIntent contract (2026-07-17):** `WidgetIntentContractExtensionTests` — reject unknown (snapshot unchanged), accept known, preserve `.userPaused` / `.playing` + language update, empty-session success (symmetric to home-widget switch SSOT and LA toggle empty-session path). Methods: `testPerformLiveActivityStreamSwitchRejectsUnknownLanguage`, `testPerformLiveActivityStreamSwitchAcceptsKnownLanguage`, `testPerformLiveActivityStreamSwitchPreservesUserPausedAndUpdatesLanguage`, `testPerformLiveActivityStreamSwitchFromPlayingUpdatesLanguageOnly`, `testPerformLiveActivityStreamSwitchSucceedsWithEmptySessionSnapshot`.
- [x] **displayFlag / displayLanguageName pure helpers (2026-07-17; WidgetSurface home 2026-07-18):** Pure ``displayFlag(for:)`` and ``displayLanguageName(for:preferredStreamLanguage:)`` live in `WidgetSurface/WidgetLanguageDisplay.swift` (`WidgetSurfaceTests` matrix). Stream-catalog preference for ``displayLanguageName(for:)`` remains the membership-exception wrapper (extension-profile `WidgetDisplayModelsExtensionTests`: `testDisplayLanguageNamePrefersAvailableStreams`, stream.flag parity).

**Tier 5 complete when:** Tier 2 checklist is green, `LutheranRadioWidgetTests` ships under the extension compile profile (**done 2026-07-15** — coordinator SSOT + perform SSOT; LA switch + display helpers closed 2026-07-17; suite grew with home live chrome, reboot distrust, and LA ensure coverage — **94** `func test` methods under `LutheranRadioWidgetTests` as of 2026-08-06), and all touched files carry production `///` docs with `SeeAlso:` to this roadmap.

---

## Identified Candidates for Future Consolidation

Authoritative inventory (no behavior change until device-proven). Mirrors Event-Driven Roadmap Tier 4 style.

**1. Optimistic snapshot & lifecycle surfaces (permanent)**

- ``persistOptimisticWidgetSnapshot(_:language:)`` — widget/LA optimistic instant feedback; permanent infrastructure, not a removal target.
- `forceStaleLivenessTimestampForTermination` — process-lifecycle sentinel; not a player event.
- `refreshVisualStateFromPersistence` / `syncVisualStateFromPersistence` — main-app cold launch and Control App Group–nil fallback; happy-path Providers do **not** hop.

**2. Dual-path `refreshIfNeeded` (event mutation + imperative lifecycle)**

Classified by ``WidgetRefreshTrigger``; inventory in Tier 3 table above and Event-Driven Roadmap Tier 4 §2. Deleting either family requires an explicit product decision after device-proven parity.

**3. Provider paint (snapshot resolve — no happy-path actor hop)**

Home and Control Providers paint via ``WidgetProviderSnapshotResolver/resolveFromSnapshot()`` (session + privacy-gated ``homeWidgetLiveChrome``). ``getPendingOrCurrentState`` and ``resolveWithActorHygiene`` are removed. ``refreshVisualStateFromPersistence()`` remains only on Control when the App Group suite is unavailable, plus main-app cold launch. Timeline requests are WidgetKit-driven, not a CPU poll loop.

**4. Fallback timers (non-candidates or demoted)**

- LA `updateTimer` — demoted; not started by default.
- ViewController widget-action polling (30 s) — complements Darwin; Tier 3 audit.
- ViewController connectivity timer (5 s) — unrelated to widget presentation.

**5. Surfaces already event-driven or complete**

- `WidgetRefreshManager` `PlayerEvent` observer.
- `RadioLiveActivityManager` `contentUpdates` via `WidgetEventObserver`.
- Provider pre-derivation of three presentation surfaces.
- Session teardown + post-stop hygiene.

**Selection criteria:** Identical to Event-Driven Roadmap Tier 4 — multiple independent consumers, weeks on device, tiny isolated edits, documentation upgrade, full build + test gates.

---

## Selecting and Implementing Micro-Steps

The next item is always the highest-priority unchecked entry in the backlog above.

**Recommended starting order (2026-07-18):**

1. ~~**`LutheranRadioWidgetTests` target**~~ — **Done**: extension-profile target + pure `WidgetSurfaceTests`.
2. ~~**Permanent-doc closeout for WidgetSurface + extension-profile tests**~~ — **Done**: `CODING_AGENT.md` / `Agents.md` two-layer cross-target section; README SSOT + verification for widget unit targets.
3. ~~**LA switch + display helper contracts**~~ — **Done (2026-07-17):** ``LiveActivitySwitchStreamIntent`` perform SSOT + ``displayFlag`` / ``displayLanguageName`` pure tests.
4. ~~**Pure presentation extraction into WidgetSurface**~~ — **Done (2026-07-18; intent execution split 2026-07-23):** ``displayFlag(for:)``, pure ``displayLanguageName(for:preferredStreamLanguage:)``, and ``WidgetProviderPresentationAssembly`` live in WidgetSurface; membership-exception ``WidgetDisplayModels.swift`` retains ``SharedPlayerManager``-coupled snapshot hygiene; ``WidgetIntentExecution`` lives in `WidgetIntentExecution.swift`.
5. ~~**Live Activity language chrome SSOT**~~ — **Closed (2026-07-19):** `ContentState.currentLanguage` + durable language mirror + chrome from `context.state`; home-widget write suppression unchanged. Deferred-recreation eligibility + soft ensures + foreground owned-surface language ensure closed later.
6. Tier 1 optional `WidgetDisplayProjection` bundle (only if future call-site churn warrants it).
7. Optional later: XCUITest widget intents / manual QA matrix (high device cost).
8. Optional later: further membership-exception reduction for ``WidgetRefreshManager`` only if coupling can be broken without a circular WidgetSurface ↔ SharedPlayerManager dependency.

Each micro-step: read target files first, minimal diff, apply the documentation standards above, update this roadmap Completed + Update Log, run build + test gates per `CODING_AGENT.md`.

An item is marked complete only when wired, documented, and the project compiles with zero warnings.

---

## Usage of This Document

This file is the **single source of truth for widget-specific backlog and architecture status**.

Contributors consult it when touching:

- `WidgetSurface/` (presentation types, intent coordinators, timeline factory, liveness policy)
- `LutheranRadioWidget/` (home widgets, Control widget, LA views, intents)
- `WidgetDisplayModels.swift` (provider resolver / snapshot hygiene)
- `WidgetIntentExecution.swift` (AppIntent `perform*` SSOT and side effects)
- `WidgetRefreshManager.swift`
- Widget-related surfaces in `SharedPlayerManager.swift` (persistence, intents, liveness, ``persistOptimisticWidgetSnapshot``)
- Widget-adjacent tests

When a backlog item splits or priorities shift, update this document in the same change.

For player **event** emission and `PlayerEvent` consumers, use [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md).

For presentation mapping rules and termination invariants, use [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md).

---

## Update Log

- **2026-08-24:** **Privacy-clear UIAlert waits for the sleep-timer confirmationDialog container** — ``ViewController/presentCoordinatorAlertAfterOutgoingPresentationSettles(_:)`` presents coordinator alerts after the SwiftUI `.confirmationDialog` container has disappeared, then one run-loop hop + `layoutIfNeeded`. ``confirmAndClearLocalState()`` does not add a second `DispatchQueue.main.async`. Does **not** disable the confirmationDialog. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) (Main-App Chrome Authority). No new XCTest. No Core / DNS / pin change.
- **2026-08-24:** **Engine construction does not activate AVAudioSession** — ``DirectStreamingPlayer`` inits do not fire-and-forget ``setupAudioSession()``. Factory-reset ``deactivateAudioSessionAsync()`` (Now Playing phase 2) therefore cannot race init ``setCategory``. First special-tuning clip and ``play()`` already await ``configureAudioSessionAsync()``. Does **not** configure the session from `ViewController`. Gate: `testConfigureAndSetupAudioSessionNoOpUnderUITestMode`. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) (user-initiated main open). No Core / DNS / pin change.
- **2026-08-23:** **Presentable scene owns cold auto-play** — Factory hygiene (``resetToFactoryDefaultsOnLaunch()``, model-only stream, `.prePlay` UI, mailbox arm) still runs immediately, including after jetsam / last-media background relaunch. Special tuning + ``play()`` run once on the first ``sceneDidBecomeActive`` after that reset (or immediately when the scene is already `.active`). Handshake: ``RadioPlayerCoordinator/markPresentableColdLaunchPlaybackReady(initialStream:)`` after hygiene, ``RadioPlayerCoordinator/notePresentableSceneForColdLaunchPlayback()`` on become-active. Sticky this-process intent still blocks. ``configureNowPlayingControlsIfNeeded()`` installs remotes at process start without calling ``play()``. Does **not** gate ``play()`` on `lastUpdateTime`. Gates: `testPresentableColdLaunchWaitsUntilSceneIsNoted`, `testPresentableColdLaunchRunsOnceWhenReadyAndPresentable`, `testPresentableColdLaunchAllowsBecomeActiveBeforeHygieneReady`, `testPresentableColdLaunchSkipsPlayWhenStickyIntent`, `testFactoryResetInstallsNowPlayingRemotesWithoutStartingPlay`. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). No Core / DNS / pin change.
- **2026-08-23:** **Factory-idle `.prePlay` does not request a missing Live Activity** — ``sessionNeedsInteractiveLiveActivity`` is false when visual is `.prePlay` without ``isConnectingPlayback`` and without stream-switch hold. Become-active ``ensureInteractiveLiveActivityIfNeeded()`` must not ``startActivity()`` a Connecting card before ``play()`` enters the start pipeline. Connecting attach and this-process ``.userPaused`` still need chrome. Do not arm ``isPlaybackStartPipelineActive`` before ``play()``. Missing-card start policy only; never end while request is ineligible. Background auto-start remains `isPlaying`-gated. Gate: `testForegroundEnsureStartPolicy`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-24:** **Widget gallery section name localized** — `INFOPLIST_KEY_CFBundleDisplayName` is `"Lutheran Radio"` (not the target identifier `LutheranRadioWidget`). `LutheranRadioWidget/InfoPlist.xcstrings` ships `CFBundleDisplayName` for all 32 UI languages in lockstep with `"lutheran_radio_title"`. WidgetKit gallery **section** chrome is InfoPlist, not Localizable; per-widget `.configurationDisplayName` is unchanged. Gate: `WidgetExtensionDisplayNameLocalizationTests`. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md), README Localizations. No Core / DNS / pin change.
- **2026-08-23:** **Same-stream warm cluster reuse on hard-resume** — ``urlWithOptimalServer(for:allowSameStreamWarmReuse:)`` skips the EU/US ping pair for ``PlaybackAttachContext/resume`` while ``lastServerSelectionTime`` is inside ``sameStreamWarmServerReuseInterval`` (10 minutes). The 10 s throttle still applies to every context. Stream switch, cold launch, and recovery ``play()`` do not use the warm window. Network-path reconnect nils the stamp. User pause still hard-tears Icecast. Does **not** skip Core pins or DNS TXT. Gate: `testWarmWindowReusesOnlyWhenSameStreamResumeIsAllowed`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-22:** **In-flight identical sticky-pause widget wake** — ``WidgetRefreshManager/shouldCoalesceInFlightIdenticalPauseRefresh`` drops later identical ``.userPaused`` / thermal / security-lock ``refreshIfNeeded`` calls (same visual + language + error + paint epoch) while the first wake is still in flight. ``lastKnownState`` stays booked only after a real WidgetCenter wake so a discarded first path cannot strand later honest pauses. Dual-path ``WidgetRefreshTrigger`` (event + teardown) is unchanged. The first paint-epoch exception after ``liveChromeIdentitySkipWake`` still executes once; concurrent duplicates of that extra wake coalesce here. Two-kind reload list unchanged. Reload remains wake-only; Providers paint session + ``homeWidgetLiveChrome``. Does **not** treat scheduler labels as lock-screen paint. Gates: `testShouldCoalesceInFlightIdenticalPauseRefreshPolicy`, `testRefreshIfNeededCoalescesInFlightIdenticalUserPausedBurst`, `testRefreshIfNeededCoalescesInFlightIdenticalPauseAfterPaintEpochAdvance`. Canonical: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §8, [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). No Core / DNS / pin change.
- **2026-08-22:** **Ineligible stream-switch Connecting after dest language landed** — ``shouldSuppressConnectingContentPushWhileIneligible`` skips `Activity.update` Connecting (``.prePlay``) over owned ``.userPaused`` / ``.playing`` while request is ineligible, including during stream-switch hold after dest language has landed. Dest-lag remains language-only (``shouldPreserveOwnedVisualOnIneligibleLanguageMutation``). Eligible switch still Connecting via ``isInteractiveLiveActivityRequestEligible``. First start (owned already Connecting) still Connecting. Unlock-heal remains the presentable playing-glyph repair. Does **not** invent `.playing`; does **not** end while ineligible; does **not** skip Connecting on an eligible switch. Gate: `testIneligibleStreamSwitchDoesNotPushConnectingAfterDestLanguageLanded`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-22:** **Ineligible freeze must not spend visual-differing playing IPC** — ``shouldSuppressVisualDifferingPlayingContentPushWhileIneligible`` skips `Activity.update` candidate `.playing` while request is ineligible after freeze soft-budget exhaust **or** over a committed ``.userPaused`` / ``.playing`` glyph. Play re-arm may still clear quiet for a later eligible cycle; it does not authorize this IPC. Language-only and pause honesty still update. Dual-axis settle after hold clear while owned is Connecting (freeze not exhausted) still may push `.playing`. Unlock-heal remains the presentable playing-glyph repair. Does **not** invent `.playing`; does **not** end while ineligible. Gate: `testIneligibleFreezeDoesNotPushVisualDifferingPlaying`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-22:** **Ineligible language mutation is language-only** — ``shouldPreserveOwnedVisualOnIneligibleLanguageMutation`` keeps owned ``.userPaused`` / ``.playing`` via ``ContentState/replacingCurrentLanguage(_:)`` when request is ineligible and dest language ≠ owned language, including during stream-switch hold. Language ensure 3/3 uses that candidate (do not spend attempts on Connecting+language). Eligible switch, first start, and dest matching owned still publish Connecting. Same-stream ineligible Connecting skip unchanged. Does **not** invent `.playing`; does **not** end while ineligible; does **not** skip Connecting on an eligible switch. Gate: `testIneligibleLanguageMutationPreservesOwnedVisualInsteadOfConnecting`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **Post-quiet language long-horizon stays language-only after freeze** — ``shouldKeepOwnedVisualOnPostQuietLanguageLongHorizon`` skips dual-axis long-horizon after freeze soft budget / playing quiet while request is ineligible. Dest language rides ``ContentState/replacingCurrentLanguage(_:)`` so candidate visual equals owned visual. Dual-axis settle at ``setPlaying()`` after hold clear stays. Playing long-horizon remains the visual rail. Stream-switch hold still Connecting. Does **not** invent `.playing`; does **not** end while ineligible. Gate: `testPostQuietLanguageLongHorizonStaysLanguageOnlyAfterFreeze`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **Same-stream ineligible resume does not spend an apply on Connecting** — ``shouldSuppressConnectingContentPushWhileIneligible`` skips `Activity.update` Connecting (``.prePlay``) over owned ``.userPaused`` / ``.playing`` while request is ineligible and stream-switch hold is inactive. Stream-switch hold still publishes Connecting + destination language. First start and request-eligible still publish Connecting. Durable mirrors may still warm Connecting. Does **not** invent `.playing`; does **not** skip Connecting on language switch. Gate: `testSameStreamIneligibleResumeDoesNotPushConnectingOverPausedOrPlaying`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **One outstanding visual mutation while apply is in-flight** — ``shouldCoalesceVisualDifferingContentPushWhileInFlight`` skips a second visual-differing `Activity.update` while ``inFlightContentPushCandidate`` is unconfirmed; ``pendingCoalescedContentPushCandidate`` replaces (do not queue), including pause replacing playing-ensure. Language-only same visual as in-flight and owned still updates. ``flushCoalescedContentPushIfNeeded`` pushes once after delayed re-read, `contentUpdates`, or immediate post-await match. Duplicate unconfirmed playing pushes also coalesce. Soft-ensure attempt counters still bound the loop. Gate: `testVisualDifferingContentPushCoalescesWhileInFlight`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **Delayed re-read axis-heal + `contentUpdates` yield diagnostics** — Stall truth is ``contentUpdates`` **or** delayed re-read; **both** run ``applySystemContentUpdateHeal`` with prior ``lastSystemHeldContent`` captured before ``commitContentPushObservation``. Immediate post-await does not axis-heal. Language-new delayed observation with visual still Connecting re-arms playing ensure. DEBUG ``handleActivityContentUpdate`` logs a rate-limited yield line. Gates: `testDelayedRereadObservationRunsAxisHealOnLanguageNewVisualLag`, `testContentUpdatesYieldDiagnosticsAreRateLimited`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **Do not replace a Live Activity that is already owned** — ``interactiveLiveActivityStartDisposition`` returns ``.updateOwned`` whenever this process already owns an interactive id (eligible or not). Eligible + unowned may `Activity.request`; ineligible + unowned records pending ensure (no request, no leading end). ``startActivity()`` defends the same shape and ends residual siblings with ``.immediate`` before a true new request (never ``.default``). Recreation still ends first with ``.immediate`` so start sees unowned. Gate: `testInteractiveLiveActivityStartRequiresRequestEligibility`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-21:** **Live Activity stall oracle, start eligibility, handshake, post-clamp playing** — Immediate `content.state` after `await Activity.update` is apply-in-flight; stall / quiet / recreation wait for ``contentUpdates`` or ``contentPushApplyConfirmationDelayMilliseconds``. Pause↔Connecting and Connecting↔playing after hold/connect clear do not consume ``stalledContentPushRecreationThreshold``. ``startActivity()`` and ``refreshAllMediaSurfaces`` `.startOrUpdate` consult ``interactiveLiveActivityStartDisposition`` (owned → update only; eligible + unowned may request; ineligible + unowned → pending ensure, no request, no leading end). Playing quiet does not starve post-clamp `.playing` after Connecting hold clears; ``resolveContentPushVisual`` still clamps during hold. Unlock-heal remains the presentable safety rail. Gates: `testContentPushStallOracleWaitsForApplyConfirmation`, `testConnectingPlayingHandshakeDoesNotConsumeRecreationStreak`, `testInteractiveLiveActivityStartRequiresRequestEligibility`, `testPlayingOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-20:** **Stream switch supersedes Connecting start latch** — ``resetToPrePlayForNewStream`` clears ``isPlaybackStartPipelineActive`` so a follow-on ``play()`` attaches the destination instead of skipping as a duplicate of the previous language’s in-flight start. In-app skip-reset play continuation clears the same latch. Same-stream Connecting still no-ops ``userRequestedPlay()``. Hold still claims resign-active preserve. Gate: `testResetToPrePlayForNewStreamClearsSupersededStartPipelineSoFollowOnPlayAttaches`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No Core / DNS / pin change.
- **2026-08-20:** **Process-exit latch keeps Home passive after swipe-up quit** — After ``hasCompletedProcessExitSessionTeardown()``, this process does not bump `lastUpdateTime` or restamp ``homeWidgetLiveChrome``. Overlapping ``sceneDidEnterBackground`` ``saveCurrentState()`` (identical-snapshot skip still bumped) can no longer restore interactive factory Connecting. ``isMainAppProcessRecentlyActive()`` re-syncs the App Group suite on read. Gate: `testProcessExitTeardownLatchSuppressesLivenessBumpAndLiveChromeRestamp`. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md) Cleanup Invariant. No Core / DNS / pin change.
- **2026-08-15:** **Control residual-play refuse** — ``performControlWidgetToggle(isPlayingRequested:)`` refuses `true` when ``shouldDistrustDurableMirrorPlayPlanning()`` is true or ``isMainAppProcessRecentlyActive()`` is false (same flags as ``performHomeWidgetToggle()`` / ``performHomeWidgetPlay()``). No optimistic snapshot, no pending play, no Darwin. Control `false` still stamps pause. ``planControlWidgetToggle(isPlayingRequested:)`` remains the raw bool matrix. After user-initiated main open, factory reset records this boot so a real Control tap still plays. Gate: `WidgetIntentContractExtensionTests` Control refuse / trusted-play matrix. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). No Core / DNS / pin change.
- **2026-08-15:** **Optional home force-quit residual (docs absorb)** — After process death without `applicationWillTerminate` and past the 60 s `lastUpdateTime` window, home paints passive `tap_to_open` (not prior-process playing); user-initiated open auto-plays and live chrome tracks the new session. Required §10.3 not re-run. Canonical: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §10.3 / §14. No product change.
- **2026-08-14:** **Provider snapshot index + hop-comment hygiene** — Tier 5 Provider synthesis index names live ``resolveFromSnapshot`` / assemble / actor-reload tests (deleted ``resolveWithActorHygiene`` names removed). Consolidation inventory and Event-Driven Provider section describe snapshot paint as the happy path (no ``getPendingOrCurrentState``). Source comments distinguish ``optimisticHomeWidgetVisualAfterPlayPlan`` (home) from ``optimisticVisualAfterPlayPlan`` (LA dual-tap) and drop “call refresh at the top of any Provider.” Legacy ``WidgetToggleRadioIntent`` sets ``openAppWhenRun = false`` to match direction-bound play/pause. Interactive heal ``///`` never prefers a lagging entry pause over resolve ``.playing``. No paint / hop / thrash product change.
- **2026-08-11:** **LA continuous-lock residual honesty (docs absorb)** — Thrash smart-loosen (freeze soft budget, true-axis partial coarsen, gated nested post-settled, eligible recreate after freeze/dual LH exhaust) shipped on product; permanent docs re-align thrash **closed** vs ContentState **acceptance residual still open** (multi-destination language stick, soft-resume visual freeze after churn, fire ≠ acceptance; home independent). Dual-axis settle + long-horizon already present on product. Open issue + Tier 1 checklist + stacking device-verified residual updated. No security surface change; no product change in this absorb.
- **2026-08-09:** **Home interactive LIVE + intent-miss host open closed (coalesced with Provider hop removal)** — Providers paint via ``resolveFromSnapshot()`` only (``resolveWithActorHygiene`` removed; Control keeps narrow ``refreshVisualStateFromPersistence()`` when App Group suite is unavailable). Interactive home LIVE heals residual playing/pause chrome via ``WidgetInteractivePaintHeal`` + suite paint epoch/signature + Darwin/local paint-advanced wake as **body dependencies only** — never thrash root EntryView `.id` with those tokens (mid-tap identity storm dropped AppIntent hit targets → host open without mailbox pause). Direction-bound ``WidgetPlayRadioIntent`` / ``WidgetPauseRadioIntent`` (``openAppWhenRun = false``); stable direction Button identity + expanded hit target; no dual ``reloadAllTimelines`` / sticky JSON rewrite on optimistic paths. Eyes-on: background home pause → Darwin + pending ``pause`` → sticky ``userPaused`` while main stays backgrounded; soft-resume play, re-pause, paused stream switch (no auto-resume). Canonical: **Provider snapshot audit** (Tier 3) + [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §6.6. No security surface change.

- **2026-08-07:** **PR5 execute-time home wake discard unification** — ``WidgetRefreshManager/refreshWouldDiscardHomeWake(executing:memory:session:isImmediate:)`` is the single execute-time SSOT (memory lag first, then session lag). Pure helpers retained; ``performRefreshIfNotStale`` one call site; memory- and session-lag rules unchanged (same discard cases as the previous sequential checks). Soft-resume non-immediate Connecting, identical non-playing coalesce, dual-path triggers, and live chrome paint SSOT unchanged. Reload remains wake-only; Providers paint session + ``homeWidgetLiveChrome``. Gate: `testRefreshWouldDiscardHomeWakeComposesMemoryThenSession`. Canonical: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md) §8. No security surface change.
- **2026-08-07:** **Home live chrome device eyes-on absorb** — Design SSOT Status → **Implemented**. Physical-device required checklist passed: cold-launch auto-play → background → home pause → soft resume → switch while playing → privacy residual clear. Design §10.3 is self-contained (product auto-play; OI-1 = residual honesty, not silent open). Optional PR5 was unblocked after eyes-on (shipped same day as separate micro-step). No product code change in the absorb commit.

- **2026-08-06:** **Inventory refresh (docs only)** — Current-state banner → 2026-08-06; extension-profile suite count → **94** `func test` under `LutheranRadioWidgetTests`; present-tense membership-exception paths list `WidgetIntentExecution.swift` separately from `WidgetDisplayModels.swift` (mechanical split; no behavior change). No production code change; no security surface change.
- **2026-07-30:** **Lock-screen language ensure status (device residual + mechanism alignment)** — Settled language acceptance is full soft-ensure re-arm after hold clear plus bounded delayed post-settled soft ensure (``postSettledLanguageEnsureDelayedIntervalsMilliseconds``), not a single dual-axis push alone. Device eyes-on: soft + delayed ensure may still exhaust without owned language acceptance for some widget **playing** switches while request stays ineligible (audio + home already destination); become-active / owned-surface foreground ensure heals flag and often playing visual on the same interactive activity without visibility-class end+request. Continuous lock-only language honesty not fully closed; write-side language SSOT and deferred-recreation eligibility remain closed. Gates: `testSettledLanguageAcceptancePushAfterHoldClearWhileQuiet`, `testPostSettledLanguageEnsureRetriesScheduleWhileOwnedLanguageLags`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression unchanged.
- **2026-07-30:** **Home live chrome permanent-doc closeout (privacy-gated ``homeWidgetLiveChrome``)** — Absorbs shipped mechanism into sibling permanent docs: Provider chrome via pure ``resolveHomeWidgetChromeFields`` (agreement → session; disagreement → fresher `updatedAt`; neither → factory); program metadata remains session → ``homeWidgetStreamMetadata``; freshness stack lists live chrome beside program-metadata mirror; ``reloadTimelines`` is wake-only (not paint payload). Terminate residual clear already on ``forceStaleLivenessTimestampForTermination`` (explicit clear, OI-1). Design SSOT: [`docs/Home-Live-Chrome-App-Group-Mirror-Design.md`](Home-Live-Chrome-App-Group-Mirror-Design.md). Presentation: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). OI-1 note: [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md).
- **2026-07-30:** **Provider session/mirror freshness** — Pure ``resolveHomeWidgetChromeFields`` so a fresher ``homeWidgetLiveChrome`` ``.playing`` beats stale extension-session switch-hold ``.prePlay`` (home yellow Connecting while main green). Fresher session pause still beats staler residual playing mirror. No Core touch.
- **2026-07-29:** **Home soft-resume Connecting chrome honesty** — Event-path ``refreshUsesImmediateDelivery`` no longer forces `immediate` for Connecting ``.prePlay`` (sticky pause/lock and ``.cleared`` remain immediate). Deferred Connecting participates in ``.prePlay`` → ``.playing`` coalesce so soft-resume schedules a single authoritative playing home reload. Independently, ``setUserIntentToPlay`` / ``clearUserPausedLockIfNeeded`` / play connecting chrome skip when ``PlaybackAttachState/canSoftResumeSameStream`` is true (gapless resume retains sticky visual until engine ``setPlaying()``). Stream-switch hold and true attach still use Connecting until audible start. Does not invent `.playing` during switch hold; privacy write suppression unchanged. Gates: `testEventPathSoftResumePrePlayIsSupersededByAuthoritativePlaying`, `testRefreshUsesImmediateDeliveryPolicyForConnectingVersusSticky`, `connectingPrePlayChromeSkippedWhenSoftResumeSameStreamAvailable`. Canonical: [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md), [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change.
- **2026-07-29:** **Home-widget program metadata honesty (privacy-gated App Group mirror + gate-open re-stamp)** — Live ICY still mutates only via ``didUpdateStreamMetadata`` (LA in-memory first). Home/Control Providers run in a separate process and cannot read main-app session RAM (OI-1 visual stays memory-only). Privacy-gated App Group ``homeWidgetStreamMetadata`` carries program title/speaker for ``WidgetProviderSnapshotResolver``; ``persistStreamMetadataForWidgets()`` / snapshot saves write the mirror when ``hasActiveWidgets``; ``restampHomeWidgetProgramMetadataAfterPrivacyGateOpenIfNeeded()`` re-stamps in-memory ICY once when the gate opens false→true (install-while-playing / identical-ICY no-op); gate close and privacy clear drop the key. Does not invent catalog titles; does not open write suppression when no widgets. Gates: `testPersistStreamMetadataSuppressedWhilePrivacyGateClosed`, `testPrivacyGateOpenRestampsInMemoryProgramMetadataForHomeWidgets`, `testPrivacyGateCloseClearsHomeWidgetProgramMetadataMirror`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md), [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md). No security surface change.
- **2026-07-29:** **Attach-path sticky Connecting snapshot quiet** — Status force-save skips sticky Connecting (``.prePlay`` / ``.cleared``) via ``DirectStreamingPlayer/shouldSkipForceWidgetSaveOnStableStatus`` (parity with sticky pause skip; error keys still force). ``SharedPlayerManager/shouldSkipIdenticalStickyConnectingSnapshotWrite`` collapses identical Connecting re-persists when language/error/metadata match; first Connecting write, language changes, error repairs, and transitions to ``.playing`` / ``.userPaused`` still write. ``saveCombinedWidgetState(language:)`` no-ops when destination is already stamped with cleared metadata. Gates: `testShouldSkipForceWidgetSaveOnStableStatusPolicy`, `testShouldSkipIdenticalStickyConnectingSnapshotWritePolicy`, `testSaveCurrentStateSkipsIdenticalStickyConnectingPersist`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Widget refresh identical non-playing coalesce (attach / dual-path quiet path)** — ``WidgetRefreshManager/shouldCoalesceIdenticalNonPlayingRefresh`` drops further `reloadTimelines` when last-known connecting chrome (``.prePlay`` / ``.cleared``) or sticky pause/lock already matches candidate visual + language + error flags. Event-path immediate delivery for sticky pause / ``.cleared`` remains (factory-reset / pause urgency); Connecting ``.prePlay`` is deferred for soft-resume coalesce (see home soft-resume entry above); language changes still force a reload; active ``.playing`` stays on adaptive debounce. Stale debounced discards via ``refreshWouldRegressPersistedSnapshot`` are intentional when a delayed target loses a race to a newer snapshot (not a dual-path failure). Dual-path architecture unchanged. Gates: `testShouldCoalesceIdenticalNonPlayingRefreshPolicy`, `testRefreshWouldRegressPersistedSnapshotPolicy`, `testRefreshIfNeededCoalescesIdenticalImmediatePrePlayAttachStorm`, `testRefreshIfNeededCoalescesIdenticalStickyUserPausedDualPath`, `testRefreshIfNeededDoesNotCoalesceIdenticalPrePlayWhenLanguageChanges`, `testRefreshIfNeededSkipsPrePlayDeferralWhenLastKnownAlreadyMatches`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Soft-ensure thrash collapse + deferred recreation announce-once** — Concurrent language/playing soft-push re-entry collapses into one in-flight loop per axis (``shouldStartAuthoritativeContentEnsureSoftPushLoop``). When stalled-push bookkeeping would recreate but request is ineligible, pending ensure is recorded once per freeze (``shouldAnnounceDeferredInteractiveRecreationWhileIneligible``); subsequent identical deferred evaluations stay quiet. Identical candidate/owned stall diagnostics and quiet-skip ensure logs rate-limit once per signature/engagement. Imperative ``updateCurrentActivity`` on true mutations unchanged. Does **not** end while ineligible; does **not** invent `.playing`. Gate: `testSoftEnsureThrashCollapseAndDeferredRecreationAnnounceOnce`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Settled language acceptance after hold clear** (mechanism later strengthened 2026-07-30 — see update log) — Soft language ensure can exhaust during stream-switch Connecting and enter quiet pending, which then defers language-only status re-pushes for the rest of a lock stretch. ``pushSettledLanguageAcceptanceContentIfNeeded()`` runs after hold clear on ``setPlaying`` / soft-resume no-op: clears language quiet and re-runs full soft language ensure for the destination; while still lagging under ineligible request, quiet re-arms and delayed post-settled soft ensure may continue without end+request; consume clears on destination change, eligibility, become-active, or `contentUpdates`. Does **not** invent `.playing` during hold; does **not** end while ineligible. Gate: `testSettledLanguageAcceptancePushAfterHoldClearWhileQuiet`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Settled playing acceptance after hold clear** — Soft playing ensure can exhaust (or cannot run usefully while hold is active) during stream-switch attach, then quiet defers visual-only `.playing` repair for the rest of a lock stretch. ``pushSettledPlayingAcceptanceContentIfNeeded()`` runs after hold/connect clear on ``setPlaying`` / soft-resume no-op when the actor is authoritative `.playing` and owned visual still lags: one dual-axis ``updateCurrentActivity`` with playing-quiet bypass once while ineligible; re-enters quiet if still lagging; consume clears on optimistic toggle / stream-switch, eligibility, become-active, or `contentUpdates`. ``setPlaying`` sequences settled language then settled playing then soft playing ensure (no blind re-arm). Does **not** invent `.playing` during hold/connect; does **not** end while ineligible. Gate: `testSettledPlayingAcceptancePushAfterHoldClearWhileQuiet`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Playing ensure quiet pending while request ineligible** — After soft playing ensure exhausts without owned `.playing` acceptance while interactive request is ineligible, ``playingEnsureQuietPending`` stops status-driven thrash; visual-only `.playing` repair re-pushes defer; pause and language mutations still push. Re-arm on authoritative play mutation (``rearmPlayingEnsureQuietPending``), optimistic toggle / stream-switch, eligibility, become-active (clears quiet before soft ensure), or system `contentUpdates`. ``setPlaying`` sequences settled language + settled playing acceptance then soft playing ensure. Does **not** invent `.playing` during hold; does **not** end while ineligible. Gates: `testPlayingEnsureQuietPendingAfterMaxAttemptsWhileIneligible`, `testPlayingOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Language ensure quiet pending while request ineligible** — After soft language ensure exhausts without owned language acceptance while interactive request is ineligible, quiet pending for that destination stops status-driven thrash; language-only ``updateCurrentActivity`` re-pushes defer; visual mutations still push. Re-arm on destination change (optimistic stream-switch shared by home-widget + LA chips), eligibility, become-active (clears quiet before soft ensure), or system `contentUpdates`. Does **not** end while ineligible. Gates: `testLanguageEnsureQuietPendingAfterMaxAttemptsWhileIneligible`, `testLanguageOnlyStatusPushDefersWhileQuietPending`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Foreground owned-surface ensure recovery rail** — When ownership is non-nil after become-active / foreground, ``ensureInteractiveLiveActivityIfNeeded()`` invokes ``ensureAuthoritativeContentOnForegroundIfNeeded()`` via ``shouldInvokeOwnedSurfaceForegroundEnsure``: always consume language/playing quiet + ``pendingInteractiveLiveActivityEnsure``; debounce dual SceneDelegate hooks / rapid resign-become thrash; re-invoke when chrome still lags and request is eligible (will-enter-foreground may run before application is active). Soft path clears both quiet flags then bounded language + playing ensure; eligible-only recreation only if soft ensure still fails (never while request is ineligible). Gates: `testForegroundOwnedSurfaceSoftEnsurePolicy`, `testOwnedSurfaceForegroundEnsureInvokeAndDebouncePolicy`, `testForegroundRecreationOnlyAfterSoftEnsureFailsAndRequestEligible`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression and home-widget write gating unchanged.
- **2026-07-29:** **Deferred recreation policy documentation** — Document eligibility-gated interactive Live Activity recreation (keep surface when request is ineligible; soft language/playing ensure continues), ContentState convergence after become-active. Not a recreation-eligibility or language-SSOT regression. Do not re-open closed eligibility / soft-resume ensure items. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). Docs-only; no production behavior change; no security surface change.
- **2026-07-29:** **ICY StreamTitle single-owner path** — live ICY mutates only via ``DirectStreamingPlayer/safeOnMetadataChange`` → ``SharedPlayerManager/didUpdateStreamMetadata`` (idempotent on identical raw); coordinator `onMetadataChange` is in-app ViewModel only; catalog ``Stream/title`` never enters StreamTitle SSOT (language-code change clears prior ICY; station/language presentation fallbacks until real StreamTitle). Gates: `testDidUpdateStreamMetadataIdenticalRawIsIdempotent`, `testDidUpdateStreamMetadataNilClearsCatalogShapedPollution`, `testCatalogShapedStationDashLanguageParsesLanguageAsProgramTitle`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change.
- **2026-07-29:** **Widget refresh derivation language under privacy write suppression** — ``WidgetRefreshManager/refreshIfNeeded`` and ``deriveRefreshParameters`` resolve language via ``SharedPlayerManager/languageForWidgetRefreshDerivation(fallbackLanguage:)`` (session snapshot → stream attach → durable LA language mirror → main-app locale reseed) so coalesce / DEBUG `lang:` labels track the playing stream under no-widgets privacy instead of ``preferredWidgetLanguage()`` hard-default `"en"`. Does not open write suppression. Gates: `testDeriveRefreshParametersTracksStreamAttachWhenSnapshotAbsentUnderNoWidgets`, `testDeriveRefreshParametersPrefersSnapshotLanguageOverStreamAttach`, `testRefreshIfNeededResolvesPrivacyHardDefaultCallerLanguageViaStreamAttach`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change.
- **2026-07-23:** **Main-app host extraction finish** — ``RadioPlayerCoordinator`` owns `selectedStreamIndex`, sleep-timer interaction + deferred ICY metadata (`onMetadataChange` registration), cold-launch special tuning (`playSpecialTuningSound` + ``TuningSoundCoordinator``), and pending-action drain. `ViewController` is thin host (lifecycle, network, Darwin/launch shims, SwiftUI hosting). Residual UIKit volume slider removed; system volume remains SwiftUI `VolumeAndAirPlayRow` / `MPVolumeView` (`volumeSlider` UITest id). No security surface change; widget/LA pending drain and privacy gates unchanged.
- **2026-07-23:** **Liveness write policy rename** — ``bumpWidgetLivenessTimestamp(policy:minInterval:)`` uses ``SharedPlayerManager/WidgetLivenessWritePolicy`` (``.throttled`` default, ``.immediate`` for language / widget / lifecycle edges) instead of a `force:` bool. Same privacy gate and cadence behavior; naming matches non-forcing architecture language. No security surface change.
- **2026-07-23:** **Home-widget privacy residual hygiene (liveness + instant feedback)** — all main-app `lastUpdateTime` writes go through privacy-gated ``bumpWidgetLivenessTimestamp`` (including language changes); ``clearHomeWidgetLivenessAndInstantFeedbackResiduals()`` drops residual heartbeat + instant-feedback keys when ``WidgetRefreshManager/hasActiveLutheranWidgets`` closes and from privacy clear. Does not touch pending mailbox, LA durable mirrors, or `lastSecurityValidation`. Gates: `testClosingPrivacyGateClearsLivenessAndInstantFeedbackResiduals`, `testPrivacyClearRemovesLivenessInstantKeysAndSuppressesMainAppRewrites`, liveness sentinel + cold-launch factory reset. Canonical: `SharedPlayerManager.swift` App Group table. No security surface change.
- **2026-07-23:** **Retired App Group orphans** — stop writing `preferredVolume` (system volume SSOT via `MPVolumeView`; engine gain unity after attach) and `lastUserPauseTime` (pause recovery is sticky ``PlaybackIntent`` / ``canProceedWithPlayback()`` only; zero disk readers). Both keys purged by ``clearPersistedVisualStateKeysFromDisk()``. SSOT table updated. Gate: extended `testColdLaunchFactoryResetClearsDiskVisualStateAndReturnsPrePlay`. Canonical: `SharedPlayerManager.swift` App Group table. No security surface change.
- **2026-08-07:** **Non-decisional pause-barrier / write-only cleanup** — removed unused in-actor recent-pause timestamp barrier (`wasRecentlyUserPaused`, `lastUserPauseTimestamp`, `recentUserPauseBarrier`, `recordUserPauseTimestamp`), write-only `hasProcessedExplicitUserPlayRequest`, and unused `StreamingSessionDelegate.connectionStartTime`. Sticky ``PlaybackIntent`` + App Group purge of `lastUserPauseTime` retained. No security surface change.
- **2026-07-23:** **Stream-switch LA language with Connecting hold** — ``resetToPrePlayForNewStream(connectingLanguageCode:)`` + ``liveActivityLanguageCodeForContentPush()`` so Live Activity ContentState never shows prior-language chrome for one push while visual is already `.prePlay` and ``selectedStream`` is still the old stream. Coordinators / Siri intents pass destination language on hold. **DEBUG latency timeline single console sink** — ``MediaTransportLatencyTimeline`` emits via `print` only (no twin `os.Logger` line). Gates: `testStreamSwitchHoldContentLanguageMatchesDestinationBeforeEnginePrep`, extended soft-silence milestone count. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; home-widget write suppression unchanged.
- **2026-07-28:** **Stream-switch snapshot language with Connecting hold** — ``PersistedLanguageResolution/resolve`` accepts hold-time `connectingLanguageCode` that outranks preferred/snapshot/model while ``holdPrePlayVisualUntilPlayback`` is active; ``saveCurrentState()`` wires ``streamSwitchConnectingLanguageCode`` so App Group snapshot language matches LA destination chrome before the engine model settles. Gates: `streamSwitchHoldConnectingDestinationOutranksLaggingPreferredSnapshotAndModel` (+ related pure resolver cases). Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression unchanged.
- **2026-07-28:** **Intentional English vs hard-default `"en"` repair** — preferred `"en"` is kept when the engine model is already `"en"` (lagging non-en snapshot must not clobber intentional English); hard-default pollution still repairs when model is non-en. Gates: `keepsIntentionalEnglishWhenModelIsEnglishDespiteLaggingSnapshot`, `repairsHardDefaultEnglishFromSnapshotWhenModelIsNonEnglish`, `repairsHardDefaultEnglishFromModelWhenSnapshotAlsoEnglish`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change.
- **2026-07-28:** **Awaited destination language on stream-switch paths** — ``updateUserDefaultsLanguage`` awaits ``saveCombinedWidgetState(language:)`` (no fire-and-forget Task as sole destination writer); switch orchestrators await before media refresh / further saves; ``saveCurrentState()`` resolves language after suspension so concurrent destination writes are not clobbered by a stale re-resolve. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression unchanged.
- **2026-07-28:** **Paused stream-switch destination language stamp** — sticky-paused language switches call ``stampStreamSwitchDestinationLanguage`` (no Connecting hold / no auto-resume) so ``PersistedLanguageResolution/resolve`` and ``liveActivityLanguageCodeForContentPush()`` outrank lagging preferred/snapshot/model; post-stamp ``clearStreamSwitchDestinationLanguageIfNotHolding()``. Gates: pure resolver hold-inactive destination cases + `testPausedLanguageSwitchDestinationStampAdvancesContentLanguageWithoutHold`. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md). No security surface change; privacy write suppression unchanged.
- **2026-07-22:** **Retired App Group key purge hygiene** — drop `migrateLegacyIsPlayingIfNeeded` alias; ``clearPersistedVisualStateKeysFromDisk()`` sole purge entry point (includes bare `currentLanguage`); SSOT table marks `isPlaying` / bare `currentLanguage` purged only; ``preferredWidgetLanguage()`` hard-defaults to `"en"` when no home widgets (no bare-key read). Canonical: `SharedPlayerManager.swift` App Group table, [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md) (OI-1). Gates: cold-launch factory reset + `testPreferredWidgetLanguageIgnoresRetiredBareCurrentLanguageKey`. No security surface change.
- **2026-07-22:** **Retire `forcePersistVisualState` alias + optimistic-snapshot SSOT alignment** — public API is ``persistOptimisticWidgetSnapshot(_:language:)`` only (deprecated forwarding wrapper removed; zero call sites). Present-tense SSOT tables and architecture docs use the optimistic-snapshot name; permanent infrastructure language for cross-process optimistic writes. Canonical: `SharedPlayerManager.swift`, [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md), ``<doc:Architecture>``, [`docs/Event-Driven-Refactor-Roadmap.md`](Event-Driven-Refactor-Roadmap.md). No security surface change; no behavior change.
- **2026-07-21:** **ICY StreamTitle typographic dash parse + Now Playing chain gates** — ``StreamProgramMetadata/from(rawICYMetadata:)`` normalizes spaced en/em dashes (U+2013 / U+2014) to ASCII `" - "` and re-joins multi-segment program titles; unit coverage includes separator matrices and raw-ICY → ``nowPlayingDisplayStrings(...)`` chains that keep speaker attribution on the system artist line (`testNowPlayingDisplayStringsFromEnDashRawICYYieldsSpeakerArtistLine`, `testNowPlayingDisplayStringsFromEmDashRawICYYieldsSpeakerArtistLine`). Tier 5 Now Playing formatter index updated. Presentation-only; no security surface change. SeeAlso: `WidgetSurface/StreamProgramMetadata.swift`, [`docs/Widget-Presentation-Dataflow.md`](Widget-Presentation-Dataflow.md).
- **2026-07-20:** **DEBUG media-transport latency timeline** — ``MediaTransportLatencyTimeline`` (membership-exception, DEBUG-only) marks LA toggle, mailbox enqueue/execute, soft silence, authoritative playing, and pending-action drain; console `[MediaTransportLatency]` lines with `t=` / `dt=`; unit gates in `SharedPlayerManagerEventTests`; no transport policy change. Canonical: [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md).
- **2026-07-27:** **Dynamic Island ActivityKit layout compliance** — Removed `ScrollView` from expanded DI; language chips moved to expanded `.bottom` as non-scrolling flag-only chips capped by ``liveActivityDynamicIslandAlternativeStreamMaxCount`` (3); center is status + fixed single-line metadata only; trailing is a single play/pause control. Eliminates the OS yellow layout-compliance overlay on expanded Dynamic Island (not related to ``PlayerVisualState.prePlay`` chrome). See `LiveActivityPresentationChrome.swift` header + docs/Widget-Presentation-Dataflow.md.
- **2026-07-19:** **Live Activity language chrome SSOT closed** — `ContentState.currentLanguage` pushed from ``mainAppLiveActivityLanguageCode()`` (stream attach); Lock Screen / Dynamic Island hoist `context.state.currentLanguage` only; durable ``liveActivityCurrentLanguage`` mirror (not gated by ``hasActiveWidgets``); optimistic play/pause uses ``languageForLiveActivityOrWidgetOptimistic()``; language-only changes force ActivityKit update; unit gates in WidgetSurfaceTests, RadioLiveActivityManagerTests, LutheranRadioWidgetTests, SharedPlayerManagerEventTests. Event-Driven roadmap records the same shipped contract (not event Tier 4). Home-widget write suppression unchanged.
- **2026-07-18:** Open issue + Tier 1 backlog for **Live Activity language chrome SSOT** — Lock Screen / Dynamic Island flag and language name re-derive via ``preferredWidgetLanguage()`` while `ContentState` carries only visual + metadata; under memory-only session snapshot + no home widgets (write suppression) resolution hard-defaults to `"en"` even when the engine stream is non-English. Documented required end state (language on `ContentState`, chrome from `context.state`, optimistic toggle language, optional durable mirror), explicit non-goals (no privacy regression), micro-steps, and file list so a later session can implement without external repro notes. Presentation derivation table extended with language-chrome row. Cross-link: Event-Driven Refactor Roadmap (presentation / cross-process; not event emission).
- **2026-07-18:** Pure presentation extraction into WidgetSurface — `WidgetLanguageDisplay.swift` (`displayFlag`, pure `displayLanguageName(for:preferredStreamLanguage:)`), `WidgetProviderPresentationAssembly.swift` (pure Provider slice assembly); membership-exception `WidgetDisplayModels.swift` slimmed to stream-catalog wrappers, ``WidgetProviderSnapshotResolver`` hygiene, and ``WidgetIntentExecution``. Canonical agent docs (`CODING_AGENT.md` / `Agents.md`, `README.md`) document the refined boundary. Pure assembly + language chrome covered in `WidgetSurfaceTests`; stream-catalog contracts remain in main-app / extension-profile suites. `WidgetRefreshManager` stays membership-exception (event observation + WidgetKit).
- **2026-07-17:** Joined optimistic-signal → main-app-drain → authoritative-state contracts indexed — two-phase play/pause round-trips, post-drain `WidgetRefreshManager` refresh gate observation without WidgetCenter IPC, and UITestMode no-bypass safety after the full optimistic signal shape; methods in `WidgetIntentContractTests` (Tier 2 checklist + Tier 5 joined-round-trip index). Complements isolated drain and optimistic-only joints already listed under play/pause drain.
- **2026-07-17:** Close last widget contract holes — ``LiveActivitySwitchStreamIntent`` extension-profile contracts (reject/accept, pause/play language update, empty-session success) symmetric to home switch + LA toggle; pure ``displayFlag`` / ``displayLanguageName`` matrix in `WidgetDisplayModelsExtensionTests`; `docs/widget-test-gaps-analysis.md` aligned (extension target + LA perform + display helpers no longer listed open).
- **2026-07-15:** Permanent-doc closeout for `WidgetSurface` + extension-profile tests — `CODING_AGENT.md` / `Agents.md` cross-target section rewritten for `WidgetSurface` + membership-exception SSOT; README SSOT, widget functionality table, and Agent Verification Commands document `LutheranRadioWidgetTests` / `WidgetSurfaceTests` (default test plan); `WidgetSurface` target sets `SWIFT_STRICT_MEMORY_SAFETY = YES` parity with `Core`.
- **2026-07-15:** LA toggle power-on hygiene — factory reset clears durable mirror + records boot identity; ``shouldDistrustDurableMirrorPlayPlanning()`` (termination sentinel or reboot) blocks durable-mirror-alone **play**; ContentState still trusted; coordinator + extension + factory-reset tests.
- **2026-07-15:** Lock-screen LA toggle multi-source planning — ContentState + durable App Group mirror before extension actor defaults; ``resolveLiveActivityToggleVisualState``; mirror write on every LA push; regression tests for empty-session pause plan.
- **2026-07-15:** `LutheranRadioWidgetTests` extension-profile target (no `LUTHERAN_MAIN_APP`; links WidgetSurface + Core; SPM membershipExceptions); ``WidgetIntentExecution/perform*`` AppIntent SSOT; extension-profile suite + WidgetSurfaceTests green; widget extension unit test coverage closed. Canonical references only (no temporary handoff docs). (Suite size grew with LA toggle/switch + display helpers; see 2026-07-17 log.)
- **2026-07-14:** Documentation standards — removed temporary handoff-doc cross-links; renamed widget extension test gap from internal tracking label to **Widget extension unit test coverage**; production `SeeAlso:` cites canonical roadmap and presentation dataflow only.
- **2026-07-14:** WidgetSurface coordinator layer — `WidgetIntentCoordinators`, `WidgetTimelineEntryFactory`, `WidgetLivenessPresentation`, `WidgetNowPlayingDisplay`; `WidgetIntentExecution` in `WidgetDisplayModels.swift`; extension `perform()`/Provider thin delegates; toggle mirror helpers removed; widget extension test coverage partially complete (coordinator SSOT in main-app host).
- **2026-07-14:** Now Playing formatter tests — `StreamProgramMetadataTests` adds `nowPlayingDisplayStrings` matrix + widget title alignment; `SharedPlayerManagerEventTests` adds `testRefreshAllMediaSurfacesOrdersNowPlayingBeforeWidgetRefreshAndWritesDisplayStrings` (DEBUG coordination-order log + NP bypass seam); Tier 2 play/pause drain method names indexed.
- **2026-07-14:** Provider synthesis — ``WidgetProviderSnapshotResolver/assemblePresentationSlices(from:)`` + `WidgetProviderPresentationSlices`; home-widget `Provider` and Control-widget `Value` use shared assembly; `WidgetDisplayModelsTests` (26 total) adds `resolveWithActorHygiene` + entry/Value synthesis contracts.
- **2026-07-14:** Presentation mapper matrices — `PlayerPresentationMapperTests.swift` (status + control SSOT for all six `PlayerVisualState` cases); Tier 5 test index closed; `WidgetRefreshManager` test count corrected to 20.
- **2026-07-14:** Tier 4 complete — [`docs/Live-Activity-Stacking-and-Media-Surfaces.md`](Live-Activity-Stacking-and-Media-Surfaces.md) (stacking matrix, LA start policy, push-cost validation); ``refreshAllMediaSurfaces`` wrapper + call-site consolidation; dual-card stacking documented as accepted; README functionality table; `testRefreshAllMediaSurfacesCompletesAndOptionalWidgetRefreshPassesGates`.
- **2026-07-13:** Tier 3 complete — ``WidgetProviderSnapshotResolver`` + provider audit table; imperative ``refreshIfNeeded`` dedup in ``performActualSave``, ``didUpdateStreamMetadata``, ``updateUserDefaultsLanguage``; ``refreshUsesImmediateDelivery`` urgency parity on event path; removed `setupWidgetActionPolling`; Control widget Provider aligned with home-widget hygiene; tests in `WidgetRefreshManagerEventTests` + `WidgetDisplayModelsTests`.
- **2026-07-13:** Tier 2 complete — `WidgetDisplayModelsTests.swift` (resolver matrix), `WidgetIntentContractTests.swift` (pending-action dedup, instant-feedback expiry, optimistic persist contract, widget switch SSOT); `forcePersistVisualState` renamed to `persistOptimisticWidgetSnapshot` with deprecated forwarding wrapper.
- **2026-07-13:** Tier 1 — Control widget `Value` pre-derivation (`statusPresentation` + `controlPresentation` in Provider; toggle label consumes narrow fields; symmetric with `SimpleEntry`).
- **2026-07-13:** Core Principles — added **Documentation standards & canonical references** (describe what ships today; link only to committed, tracked docs).
- **2026-07-13:** Tier 1 complete — narrow family view inputs (`LutheranRadioWidgetEntryView` projection) and LA expanded-region `statusPres` dedup (outer `dynamicIsland` closure).
- **2026-07-10:** Tier 2 backlog: rename `forcePersistVisualState` → `persistOptimisticWidgetSnapshot` (permanent widget infrastructure naming); consolidation inventory updated.
- **2026-07-10:** Added **Target Architecture (Ultimate Goal)** section (two-zone hybrid model, permanent snapshot cross-process boundary, definition of “done”) and Tier 3 scope note clarifying consolidation is main-app refresh dedup only.
- **2026-07-10:** Initial roadmap drafted from codebase inventory + canonical docs (`Widget-Presentation-Dataflow.md`, `Event-Driven-Refactor-Roadmap.md`, `cold-launch-streamplay-regression-checklist.md`, `SharedPlayerManager+NowPlaying.swift`, `StreamProgramMetadata.swift`). Completed section consolidates control presentation migration, Provider/LA pre-derivation, memory-only snapshot policy, refresh/teardown/event-consumer tests, and cross-process intent SSOT. Open issues (force-quit liveness, extension event isolation, test coverage, media stacking) recorded. Backlog Tiers 1–5 populated; Tier 2 tests and Tier 1 presentation hygiene are highest priority.

---

This document is consulted at the beginning of any work on widget, Control widget, or Live Activity **functionality** (as distinct from player event emission).