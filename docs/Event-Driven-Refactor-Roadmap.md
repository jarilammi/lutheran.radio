# Event-Driven Refactor Roadmap

**Purpose**  

This document is the authoritative living record of the event-driven architecture in which **nothing is forced** and **all significant actions and state transitions flow through typed events**.

It serves as both the project backlog for remaining work and a self-contained reference for developers and coding agents. It describes the current state, the governing principles, and the next micro-steps.

**Core Principles (Never Violate)**
- Progress must be **slow and piece-by-piece**. Every micro-step must be small, reviewable, and non-breaking.
- **Nothing is forced**: Existing imperative paths, direct calls, and snapshot-based widget/Live Activity logic must continue to work unchanged. Event emission and consumption are always additive.
- All comments and documentation must be written at **production level** (present tense, final architecture language). No "phase", "step", "temporary", "will be", or migration language is allowed.
- Security invariants come first. No changes may touch `Core/`, certificate validation, DNS TXT validation, or `SecurityModelValidator`.
- Every change must follow `CODING_AGENT.md` (structured documentation, `SeeAlso`, AGENT NOTE where appropriate, build gates, etc.).
- After every micro-step, this roadmap document must be updated to reflect completed work and adjust priorities.

**Current State (as of last observed commit)**

**Completed**
- `PlayerEvent` enum introduced in `PlayerVisualState.swift` as the canonical vocabulary for player-domain events (additive only).
- `SharedPlayerManager` established as the **authoritative emitter** of `PlayerEvent`.
- Clean `AsyncStream<PlayerEvent>` implementation added (`events` property created exactly once, continuation lives for actor lifetime).
- `internal emit(_:)` central emission point (with widget-process guard).
- Emission of `.playbackIntentChanged` wired inside `updatePlaybackIntent(to:)`.
- Tier 1 Emission Coverage complete: stream transitions (start/pause/stop/fail via existing surfaces + enhanced `markPlaybackStoppedByStreamFailure`), `metadataDidUpdate` (in `didUpdateStreamMetadata` + clears), `visualStateDidChange` (via `applyVisualState` / `setVisualState`), `persistedWidgetStateDidUpdate` (after authoritative snapshot writes in `savePersistedWidgetState`). Emissions added after state mutations inside `SharedPlayerManager` using existing surfaces only; no new record APIs or emission logic in `DirectStreamingPlayer`.
- Surface cleanup in `SharedPlayerManager.swift` + `PlayerViewModel.swift` (commit d219f8e): event-driven non-forcing architecture promoted in headers and public API docs; legacy forcing shims (e.g. `forcePersistVisualState` and related) explicitly scoped to widget optimistic paths only; stronger SeeAlso/cross-links to ``PlayerEvent``, ``events``, and this roadmap.
- Tier 2 first consumer: lightweight internal observer added inside `WidgetRefreshManager` (main-app only). It reacts to key `PlayerEvent` cases and drives timeline reloads exclusively by calling the pre-existing `refreshIfNeeded(visualState:currentLanguage:hasError:immediate:)` surface with state derived from the same SSOT readers (`loadPersistedWidgetState`, `loadSharedState`). All imperative snapshot paths, debouncing, coalescing, and guards remain 100% unchanged and primary. Full structured documentation and present-tense final-architecture comments added per CODING_AGENT.md. (2026-07-04)
- Tier 2 UI subscriber: lightweight `PlayerEventSubscriber` (Observation/@State/.task/onChange/Task) added in the primary player UI hosting view (`RadioPlayerView`). Reacts to `playbackIntentChanged` + state transition events; updates local observable values for UI-only side effects (animations, refreshes, Widget/LA coordination). Existing direct bindings untouched. Full production docs + file header improvements. (2026-07-05)
- Tier 3 current-state replay implemented: `PlayerCurrentState` type + `SharedPlayerManager.currentState` snapshot and `makeEventsStreamWithReplay()` added. `PlayerEventSubscriber` wired to the replaying stream. All surfaces carry production-level documentation. Late subscribers receive current state on subscription. (2026-07-06)
- Architectural evaluation (Tier 3): per-actor `AsyncStream`s owned by the domain actor are sufficient and the chosen long-term model. `SharedPlayerManager` is the sole player-domain emitter of `PlayerEvent`. A higher-level typed event bus wrapping multiple domain actors is not required. Consumption is unified via the generic `WidgetEventObserver` helper (player events + Live Activity attribute `contentUpdates`). Core security actors remain deliberately outside the player event surface. The per-actor design preserves clear ownership, additive emission, process guards, and independent replay. (2026-07-06)
- Error and recovery observation surface complete: `streamDidFail` with classified `StreamErrorType` together with `streamDidStart` and `PlayerCurrentState.hasError` form the finalized contract. Evaluation across emitters, DirectStreamingPlayer recovery paths, and consumers confirmed no additional cases are required. Supporting documentation updated with production language. (2026-07-06)
- Tier 5 test expansion (second micro-step): live `events` stream coverage added for `playbackIntentChanged`, `visualStateDidChange`, `streamDid*` verbs, and `streamDidFail(…)` with `StreamErrorType` payload. See Tier 5 section and Update Log. (2026-07-06)
- Tier 5 test expansion (third micro-step): live coverage completed for `.metadataDidUpdate(non-nil)` and `.persistedWidgetStateDidUpdate` (plus setUp gate enablement for the privacy write path). Includes acceleration of Live Activity / WidgetCenter surfaces (guards + cheap sanitization) so coverage can be exercised without multi-minute stalls from real system service IPCs. See Tier 5 and Update Log. (2026-07-06)
- Tier 5 test expansion (fourth micro-step, partial): replay prefix contract + canonical `stop()` emission order protected by `testReplayStreamPrefixesStateThenForwardsLiveStopEmissionsInOrder`. `makeEventsStreamWithReplay()` materializes the live stream while actor-isolated and yields before return so forwarding iterators attach before live mutations. Test helpers `collectSeamEvents`, `assertEvents(containInOrder:)`, and hardened `collectEvents` / `waitForEvent` subscribe-before-action patterns. Canonical order assertions use the DEBUG notification seam; replay live-forwarding remains best-effort only. Comprehensive emission-order coverage for all transitions remains open. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (fifth micro-step): `markPlaybackStoppedByStreamFailure(_:)` emission order + intent-preservation contract protected by `testStreamFailureEmissionOrderPreservesIntentAndMutationSequence`. Asserts `visualStateDidChange(.userPaused)` → `streamDidFail(…)` → `.persistedWidgetStateDidUpdate` subsequence, negative guards (no `playbackIntentChanged`, `streamDidPause`, or `streamDidStop`), and unchanged `currentPlaybackIntent` after failure. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (sixth micro-step): canonical `setUserPaused()` emission order protected by `testSetUserPausedEmissionOrderMatchesCanonicalMutationSequence`. Asserts `visualStateDidChange(.userPaused)` → `playbackIntentChanged(.userPaused)` → `streamDidPause` → `.persistedWidgetStateDidUpdate` subsequence, negative guards (no `streamDidStop` or `streamDidFail`), and sticky `.userPaused` postconditions on visual + intent. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (seventh micro-step): metadata clear contract protected by `testMetadataClearEmitsNilWithoutPlaybackMutation`. Asserts `clearSoftPauseMetadataStashForLanguageChange()` emits exactly `.metadataDidUpdate(nil)` with no stream/visual/intent/persist emissions and unchanged playback state. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (eighth micro-step): canonical `setPlaying()` emission order protected by `testSetPlayingEmissionOrderMatchesCanonicalMutationSequence`. Asserts `visualStateDidChange(.playing)` → `playbackIntentChanged(.shouldBePlaying)` → `streamDidStart` → `.persistedWidgetStateDidUpdate` subsequence (recovery path from `.userPaused`), negative guards (no pause/stop/fail verbs), and `.playing` / `.shouldBePlaying` postconditions. `setPlaying()` UITestMode path now performs canonical mutations + emissions while still skipping Live Activity / Now Playing IPC. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (ninth micro-step): full language-switch emission paths protected by `testActiveLanguageSwitchResetThenResumeEmissionOrderPreservesIntent` (active-intent: `resetToPrePlayForNewStream` → `switchToStream` → `setPlaying`; prePlay hold + playing resume subsequence, intent preserved, no stream terminal verbs) and `testPausedLanguageSwitchFullPathClearsMetadataWithoutVisualOrIntentMutation` (paused: `switchToStream` + `clearSoftPauseMetadataStashForLanguageChange`; metadata nil only, visual/intent unchanged). See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (tenth micro-step): error/recovery late-subscriber replay protected by `testReplayPrefixAfterStreamFailurePreservesIntentThenReflectsRecovery`. Asserts replay prefix after `markPlaybackStoppedByStreamFailure(.transientFailure)` carries `.userPaused` visual with preserved `.shouldBePlaying` intent (`isBlockedByStickyIntent == false`), then a fresh replay prefix after `setPlaying()` recovery reflects `.playing` / `.shouldBePlaying` / `isActivelyPlaying`. Best-effort recovery forwarding on the first replay stream. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (eleventh micro-step): explicit-pause vs stream-failure late-subscriber contrast protected by `testReplayPrefixDistinguishesExplicitPauseFromStreamFailure`. Asserts identical grey `.userPaused` replay visual with divergent `.playbackIntentChanged` values (sticky `.userPaused` vs preserved `.shouldBePlaying`) and matching `isBlockedByStickyIntent` postconditions across both phases in one isolated test. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (twelfth micro-step): permanent-error `hasError` in replay snapshot protected by `testReplayPrefixReflectsPermanentErrorInCurrentStateSnapshot`. Asserts security-lock (`.securityLocked` visual + `hasError` / `isInPermanentError`), permanent stream failure (grey `.userPaused` + `hasError` via persisted snapshot when engine `hasPermanentError` is set), and transient-failure contrast (same grey visual with `hasError == false`). See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (thirteenth micro-step): multi-subscriber attach ordering protected by `testMultiSubscriberReplayAttachOrderingPreservesIndependentPrefixesAndLiveOrder`. Asserts attach-time replay prefix independence (early vs late subscriber), no synthesized historical `streamDid*` verbs on late attach, and identical prefixes for same-state concurrent replay streams via ``collectEventsConcurrently``. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (fourteenth micro-step): canonical `markAsUserPaused()` emission order protected by `testMarkAsUserPausedEmissionOrderMatchesCanonicalMutationSequence`. Asserts `visualStateDidChange(.userPaused)` → `playbackIntentChanged(.userPaused)` → `streamDidPause` → `.persistedWidgetStateDidUpdate` subsequence (mirrors `setUserPaused()` contract), negative guards (no `streamDidStop` or `streamDidFail`), and sticky `.userPaused` postconditions on visual + intent. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 test expansion (fifteenth micro-step): `streamDidFail(.securityFailure)` classified payload protected by `testSecurityFailureStreamDidFailPayloadIsFaithfullyEmitted`. Asserts `visualStateDidChange(.userPaused)` → `streamDidFail(.securityFailure)` → `.persistedWidgetStateDidUpdate` subsequence, exactly one fail emission carrying `.securityFailure` (no transient/permanent misclassification), negative guards (no `playbackIntentChanged`, `streamDidPause`, or `streamDidStop`), and unchanged `currentPlaybackIntent`. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 consumer coverage (sixteenth micro-step): `WidgetRefreshManager.handlePlayerEvent` visual derivation protected by `WidgetRefreshManagerEventTests.swift`. Extracts shared `deriveRefreshParameters(for:)` helper; DEBUG white-box seams `_test_deriveRefreshParameters(for:)`, `_test_handlePlayerEventBypassingUITestMode(_:)`, and `_test_setRecordHandlePlayerEventDerivation(_:)` bypass UITestMode guards without WidgetCenter IPC. Asserts carried `.visualStateDidChange` preference over stale persisted snapshots, persisted fallback for non-carrying events (`.streamDidStart`, `.persistedWidgetStateDidUpdate`), `.prePlay` default when snapshot absent, and `hasError` propagation from SSOT readers. See Tier 5 and Update Log. (2026-07-07)
- Tier 5 consumer coverage (seventeenth micro-step): `PlayerEventSubscriber` observation contract protected by `PlayerEventSubscriberEventTests.swift`. MainActor tests drive `beginObserving()` for the four-event replay prefix (`eventCount`, `lastObservedIntent` alignment with `currentState`), `_test_applyPlayerEvent(_:)` for intent vs non-intent observable update rules, and `cancel()` suppression of replay-stream delivery. DEBUG observation-suppression seams on `WidgetRefreshManager` isolate the shared `events` iterator for replay attachment. See Tier 5 and Update Log. (2026-07-07)
- Cold-launch thermal snapshot sanitization (adjacent SSOT fix, 2026-07-07): superseded by memory-only policy below.
- Memory-only visual state policy (2026-07-07): `SharedPlayerManager` no longer writes visual/playback state to UserDefaults. ``resetToFactoryDefaultsOnLaunch()`` clears on-disk `persistedWidgetState` / `playerVisualState` / legacy bools on every cold launch; `PersistedWidgetState` is an in-process session snapshot only (`inMemorySessionWidgetSnapshot`). `loadPersistedWidgetState()` never reads disk; widgets show factory `.prePlay` after relaunch. In-session thermal sanitization retained. `ViewController` cold-launch Task calls reset before resurrection guards and no longer seeds on-disk snapshots. Protected by `testColdLaunchFactoryResetClearsDiskVisualStateAndReturnsPrePlay`. Resolves Open Issue **OI-1**.
- MediaRemoteUI launch watchdog fix (2026-07-08): ``teardownNowPlayingSession()`` split into awaited phase 1 (nil `MPNowPlayingInfoCenter`, cancel pending widget work) and detached phase 2 (minimal AVPlayer detach; audio-session deactivation device-only with timeout). `isTeardownInProgress` + debounce on `SharedPlayerManager`; `WidgetRefreshManager.isSessionTeardownInProgress` suppresses `reloadTimelines` during teardown. `clearSystemNowPlayingMetadataSynchronously()` metadata-only on terminate (no synchronous AVPlayer replace). Protected by `testTeardownNowPlayingSessionClearsSystemMetadata`. Resolves MediaRemoteUI 0x8BADF00D on cold launch / terminate (simulator + device).
- Session + widget teardown follow-up (2026-07-08): ``performSessionAndWidgetTeardown()`` orchestrates factory reset (optional), ``teardownNowPlayingSession()``, Live Activity dismissal (graceful or immediate), `WidgetCenter.reloadAllTimelines()` + `WidgetRefreshManager.refreshIfNeeded(..., immediate: true)` **after** the teardown gate clears. ``performSessionTeardownSynchronouslyForTermination()`` consolidates terminate/disconnect paths. ``performPostStopWidgetHygiene()`` runs after ``stop()`` for immediate `.userPaused` widget sync. Invoked from `sceneWillResignActive` (non-playing only), `applicationWillTerminate`, `sceneDidDisconnect`, ``clearAllLocalState()``, ``resetToFactoryDefaultsOnLaunch()``, and post-stop. DEBUG `[SessionTeardown] LOCK/UNLOCK` logs trace re-entrancy and lock/inactive cycles. `refreshIfNeeded` honors `immediate: true` for `.prePlay`/`.cleared` (bypasses coalesce deferral). SeeAlso: docs/Widget-Presentation-Dataflow.md (termination invariant).

**Architecture Status**
- `SharedPlayerManager` is the single source of truth for emitting player events. It owns the canonical `PlayerEvent` vocabulary and the long-lived `events` `AsyncStream`.
- All Tier 1 `PlayerEvent` cases are emitted after their state mutations inside the actor.
- Emissions are strictly additive; direct state access, imperative paths, and widget snapshot writes remain the primary mechanism. Legacy forcing surfaces exist only for compatibility and are documented as such.
- `SharedPlayerManager` supplies `currentState` (snapshot) and `makeEventsStreamWithReplay()` for late-subscriber initialization. The primary `events` stream continues unchanged. `makeEventsStreamWithReplay()` materializes the shared live stream while already actor-isolated (avoiding a second hop before subscribe) and yields before returning so per-subscriber forwarding iterators reach their first suspended `for await` before callers drive live mutations. `PlayerCurrentState` provides convenience accessors (`isActivelyPlaying`, `isBlockedByStickyIntent`, `isInPermanentError`) that delegate to the canonical SSOT types for ergonomic replay initialization.
- The architecture uses per-actor `AsyncStream`s. `SharedPlayerManager` is the sole player-domain emitter. A higher-level typed event bus that wraps multiple domain actors is not used. Clear domain ownership, process isolation (main-app emission only), and independent replay are preserved. The generic `WidgetEventObserver` unifies consumption of `PlayerEvent` streams and framework attribute sequences (e.g. Live Activity `contentUpdates`).
- Consumers exist: `WidgetRefreshManager` maintains a lightweight internal observer of `events` (started only in the main app process) and `RadioLiveActivityManager` consumes Live Activity attribute events. Both delegate to the consolidated `WidgetEventObserver` helper. The main-app player UI layer (`RadioPlayerView`) hosts an additive `PlayerEventSubscriber` that consumes the replaying stream for UI-only effects and seeds from `currentState`. All snapshot + refresh + LA + direct-binding logic, seams, and behavior are unchanged. Event-driven and imperative paths operate in parallel.
- Widget, Live Activity, and UI paths continue to rely on direct state access and snapshot derivation as the primary mechanism. The event path (with replay) is available for decoupled observation. See player SSOT file docs for non-forcing direction and cross-references.
- Error and recovery conditions are expressed by the established vocabulary: `streamDidFail(DirectStreamingPlayer.StreamErrorType)` for classified failures (with `transientFailure` indicating recoverability) and subsequent `streamDidStart` for successful recovery. `PlayerCurrentState.hasError` provides the current terminal state to replay consumers. No additional cases are required.
- Core security actors (`SecurityModelValidator`, `CertificateValidator`) are excluded from the player event surface by design.

---

## Open Issues (Tracked Outside Tier Backlog)

These items are architectural defects or policy gaps exposed by production-like cold-launch behavior. They are recorded here so agents do not mistake a point fix for a completed restore model. They are **not** event-emission work; they belong to the `PersistedWidgetState` / resurrection policy layer documented in `SharedPlayerManager.swift` and `CODING_AGENT.md`.

### OI-1 — Cold-launch snapshot restore applies persisted visual state verbatim

**Status:** Resolved (2026-07-07) — memory-only visual policy

**Resolution**
- Visual/playback state is **never** written to UserDefaults. ``resetToFactoryDefaultsOnLaunch()`` purges on-disk keys and resets to factory `.prePlay` on every cold launch.
- `PersistedWidgetState` remains as an **in-process session snapshot** (`inMemorySessionWidgetSnapshot`) for widget refresh derivation within the current runtime only.
- `loadPersistedWidgetState()` reads memory only; cross-process widget timelines default to safe `.prePlay` after relaunch.
- In-session thermal sanitization (`sanitizedVisualStateForCrossProcessRestore`, `visualStateForPersistenceWrite`) retained for active playback.
- Regression: `testColdLaunchFactoryResetClearsDiskVisualStateAndReturnsPrePlay`.

**SeeAlso:** ``SharedPlayerManager/resetToFactoryDefaultsOnLaunch()``, ``ensureVisualStateLoaded()``, ViewController.viewDidLoad cold-launch Task, resurrection tables in `SharedPlayerManager.swift`, CODING_AGENT.md (Single Source of Truth Principles).

---

## Remaining Backlog (Prioritized for Slow, Safe Progress)

The backlog is ordered by increasing risk and decreasing isolation. Earlier items are safer and more contained.

### Tier 1 – Emission Coverage (Low Risk, High Value)
Goal: Ensure every significant domain transition inside `SharedPlayerManager` and related player logic produces the corresponding `PlayerEvent`.

- [x] Emit `streamDidStart`, `streamDidPause`, `streamDidStop`, and `streamDidFail` (via `setPlaying`, `setUserPaused`/`markAsUserPaused`, `stop`, `markPlaybackStoppedByStreamFailure` using existing surfaces: emissions added after state mutations inside the actor; no new record APIs or emission logic in `DirectStreamingPlayer`).
- [x] Emit `metadataDidUpdate` when `StreamProgramMetadata` changes (and on clears).
- [x] Emit `visualStateDidChange` when `PlayerVisualState` is updated (centralized via `applyVisualState`).
- [x] Emit `persistedWidgetStateDidUpdate` when widget snapshot state is written (after `savePersistedWidgetState` writes).
- [x] (No additional cases required; `visualStateDidChange` case was present and wired.)

**Tier 1 complete.** All core player domain events (`streamDidStart`, `streamDidPause`, `streamDidStop`, `streamDidFail`, `metadataDidUpdate`, `visualStateDidChange`, and `persistedWidgetStateDidUpdate`) are now emitted from `SharedPlayerManager` after their corresponding state mutations. Emissions are strictly additive and use existing canonical surfaces; direct state access and imperative paths remain the primary mechanism.

**Rule for these items**: Emission must be added *after* the state mutation, inside the same actor or controlled boundary. Never remove or bypass existing direct paths.

### Tier 2 – First Consumers (Medium Risk)
Goal: Introduce the first real observers of the `events` stream without forcing any existing code to change.

- [x] Add an internal observer inside `WidgetRefreshManager` that reacts to key `PlayerEvent` cases (`.visualStateDidChange`, `.persistedWidgetStateDidUpdate`, stream transitions, metadata, intent) to trigger timeline reloads by routing through the existing `refreshIfNeeded` surface. All snapshot derivation, debouncing, coalescing, regress guards, privacy gating, and direct call sites remain 100% intact and primary. The observer is strictly additive and non-forcing.
- [x] `RadioLiveActivityManager` consumes the Live Activity attribute updates AsyncStream (`activity.contentUpdates`, the events surface for `LutheranRadioLiveActivityAttributes.ContentState`). On yield the manager aligns `lastPushedContent` (stronger diff suppression of `update(using:)`) and reacts to terminal states for self-healing lifecycle. Observation starts on acquisition and is cancelled on end; all prior push sites, dedup, gates, and tests are unchanged (additive only). Production docs added. (2026-07-04)
- [x] Tier 2 polish complete: common observation pattern for `PlayerEvent` (WidgetRefreshManager) and Live Activity attribute events (RadioLiveActivityManager) extracted to internal `WidgetEventObserver` helper. Both managers delegate to the helper; all task seams (`eventObservationTask`, `activityObservationTask`), property contracts, test isolation, and observable behavior are preserved exactly. No new polling or forcing. `WidgetEventObserver` added to cross-target shared sources with full production documentation and cross-links. (2026-07-04)
- [x] Add a lightweight subscriber (`PlayerEventSubscriber`) in the main app UI layer inside the primary player hosting view (`RadioPlayerView`). Uses `@State` + `@Observable` + `.task` + `.onChange` for value-type driven reactions to `playbackIntentChanged` and all other key `PlayerEvent` cases. Existing direct state bindings, @Bindable viewModel usage, and all subview contracts remain 100% untouched. Observation reuses `WidgetEventObserver` internally; all side effects are UI-only (local state updates for future animations / refreshes / Widget/LA coordination). Full structured production `///` documentation + file header uplift applied. Non-forcing, additive only. (2026-07-05)

**Rule**: Consumers must be additive. The imperative/snapshot paths stay as the primary mechanism. Event-driven paths run in parallel initially.

### Tier 3 – Richer Event Surface & Replay (Higher Value, More Care)
- [x] `PlayerCurrentState` value type introduced alongside `PlayerEvent`. `SharedPlayerManager` now exposes `currentState` (authoritative snapshot) and `makeEventsStreamWithReplay()` (per-subscriber stream that first yields events synthesized from current state then forwards the live stream). `PlayerEventSubscriber` (RadioPlayerView) now consumes the replaying stream. All additions are strictly additive; the original `events` surface and all imperative/snapshot paths remain unchanged and primary. Full production documentation, cross-links, and present-tense final-architecture comments applied in `PlayerVisualState.swift`, `SharedPlayerManager.swift`, `RadioPlayerView.swift`, `WidgetEventObserver.swift`, and `WidgetRefreshManager.swift`. (2026-07-06)
- [x] Architectural evaluation complete: per-actor `AsyncStream`s (owned by the authoritative domain actor) are sufficient and the long-term model. A higher-level typed event bus wrapping multiple domain actors is not used. See Completed section and Architecture Status for rationale and invariants. (2026-07-06)
- [x] Error and recovery observation surface complete. `streamDidFail(DirectStreamingPlayer.StreamErrorType)` carries the classified failure (transientFailure, permanentFailure, securityFailure, unknown). Recovery success is expressed by a subsequent `streamDidStart`. `PlayerCurrentState.hasError` supplies the terminal condition for late subscribers. After inspection of emission sites, `StreamErrorType`, recovery paths in DirectStreamingPlayer, and all current consumers, no additional `PlayerEvent` cases provide further value. The existing vocabulary plus replay is the complete contract. Documentation updated in `PlayerVisualState.swift` and `WidgetRefreshManager.swift`. (2026-07-06)

### Tier 4 – Gradual Consolidation (Long-term, High Caution)
- [x] Identify places where direct "force" calls or polling can be replaced by event observation (very late stage). The complete inventory appears in the "Identified Candidates for Future Consolidation" subsection below. All findings respect the non-forcing rule: imperative snapshot paths, direct state access, and legacy forcing shims remain primary and untouched. Identification performed via exhaustive search of call sites, timers, widget entry points, coordinator/VC sync logic, and cross-process surfaces. No behavioral changes. (2026-07-06)
- [ ] Clean up any now-redundant direct mutation paths only after multiple consumers prove the event path is reliable.
- [x] Add comprehensive tests for event emission order, stream behavior under actor isolation, and late-subscriber scenarios. **Complete (2026-07-07):** `stop()` canonical emission order (`visualStateDidChange` → `playbackIntentChanged` → `streamDidStop` → `.persistedWidgetStateDidUpdate` when the write path runs) is protected via ordered subsequence matching on the DEBUG notification seam. `setUserPaused()` order (`visualStateDidChange` → `playbackIntentChanged` → `streamDidPause` → `.persistedWidgetStateDidUpdate`) is protected by `testSetUserPausedEmissionOrderMatchesCanonicalMutationSequence`. `markPlaybackStoppedByStreamFailure(_:)` order (`visualStateDidChange` → `streamDidFail` → `.persistedWidgetStateDidUpdate`, intent unchanged, no `playbackIntentChanged`) is protected by `testStreamFailureEmissionOrderPreservesIntentAndMutationSequence`. Metadata clear (`clearSoftPauseMetadataStashForLanguageChange()` → `.metadataDidUpdate(nil)` only, no playback mutation) is protected by `testMetadataClearEmitsNilWithoutPlaybackMutation`. `setPlaying()` order (`visualStateDidChange(.playing)` → `playbackIntentChanged(.shouldBePlaying)` → `streamDidStart` → `.persistedWidgetStateDidUpdate` on the recovery path) is protected by `testSetPlayingEmissionOrderMatchesCanonicalMutationSequence`. Full language-switch paths (active: `resetToPrePlayForNewStream` → `switchToStream` → `setPlaying`; paused: `switchToStream` + metadata clear) are protected by `testActiveLanguageSwitchResetThenResumeEmissionOrderPreservesIntent` and `testPausedLanguageSwitchFullPathClearsMetadataWithoutVisualOrIntentMutation`. Replay prefix-then-live ordering is protected on the four synthesized state events. Error/recovery late-subscriber replay (`markPlaybackStoppedByStreamFailure` → preserved-intent prefix → `setPlaying` recovery prefix) is protected by `testReplayPrefixAfterStreamFailurePreservesIntentThenReflectsRecovery`. Explicit-pause vs stream-failure late-subscriber contrast (identical grey visual, divergent replay intent + `isBlockedByStickyIntent`) is protected by `testReplayPrefixDistinguishesExplicitPauseFromStreamFailure`. Permanent-error `hasError` in replay snapshot (security lock + permanent stream failure vs transient contrast) is protected by `testReplayPrefixReflectsPermanentErrorInCurrentStateSnapshot`. Multi-subscriber attach ordering (attach-time prefix independence + concurrent live forwarding) is protected by `testMultiSubscriberReplayAttachOrderingPreservesIndependentPrefixesAndLiveOrder`. Replay live-forwarding on a single iterator after the prefix remains a best-effort check only in `testReplayStreamPrefixesStateThenForwardsLiveStopEmissionsInOrder` (XCTest host attach races can still drop forwarded yields).

#### Identified Candidates for Future Consolidation

This subsection records the authoritative inventory of direct "force", refresh, persistence-refresh, and polling patterns that became visible once Tier 1–3 emission + replay + first consumers were in place. It is the reference for any future Tier 4 micro-steps. All descriptions use production-level language; replacements remain strictly additive, very late stage, and gated on proven reliability of the event path across widget, Live Activity, UI, and recovery surfaces.

**1. Legacy forcing shims (widget optimistic + termination liveness)**

- `SharedPlayerManager.forcePersistVisualState(_:language:)` and private `_forceSetCurrentVisualState(_:)` (SharedPlayerManager.swift:929).  
  Nonisolated public surface that writes the authoritative `PersistedWidgetState` snapshot and performs an in-memory visual update inside the actor (widget process only).  
  Primary callers: widget AppIntent handlers (play/pause/switch paths inside SharedPlayerManager for optimistic cross-process feedback), LutheranRadioWidgetControl.swift, and internal signal paths.  
  Current status: Explicitly documented as "legacy forcing surface" and "exists solely for widget optimistic instant-feedback paths". The `emit` guard already prevents events from widget processes.  
  Late-stage note: Snapshot writes from the widget process are required for instant visibility before any Darwin round-trip. Event-driven observation is main-app only. The force path can never be removed; at most some call sites inside the main app could be audited later.

- `SharedPlayerManager.forceStaleLivenessTimestampForTermination()` (SharedPlayerManager.swift:2373).  
  Writes the `lastUpdateTime = 0` sentinel.  
  Call sites: AppDelegate.applicationWillTerminate, SceneDelegate.sceneDidDisconnect, tests.  
  Purpose: Forces passive "tap to open" presentation in widgets/Live Activities after the main process is gone.  
  Relation to events: Not a player-domain visual or intent transition; it is a process-lifecycle signal. Remains a direct, explicit lifecycle API.

- `refreshVisualStateFromPersistence()` / `syncVisualStateFromPersistence()` (SharedPlayerManager.swift:956, 971).  
  Read-side helpers that reset the one-shot `hasLoadedVisualStateFromPersistence` guard so long-lived widget extension processes see the latest main-app or forcePersist writes.  
  Call sites: Every widget Provider entry point (LutheranRadioWidget.swift:307, LutheranRadioWidgetControl.swift:174), RadioPlayerCoordinator.performColdLaunchPlaybackIfAllowed + other paths (217), ViewController cold-launch / foreground guards (428, 499), and defensive spots inside SharedPlayerManager.  
  Late-stage note: Widget extension processes have no access to `SharedPlayerManager.events`. These read-refresh surfaces are expected to remain for extension hygiene even after main-app consolidation.

**2. Imperative `refreshIfNeeded` call sites (primary path; event observer already parallels them)**

`WidgetRefreshManager.refreshIfNeeded(visualState:currentLanguage:hasError:immediate:)` is invoked directly from many surfaces in addition to the internal Tier 2 `PlayerEvent` observer (which routes through the identical public surface):

- Inside SharedPlayerManager (performActualSave, saveCurrentState, saveCombinedWidgetState, language switch handling, post-mutation sites ~2121, 2157, 3190, 3424).
- AppDelegate (foreground / launch paths ~80, 118).
- SceneDelegate (becomeActive, termination cleanup ~67, 90).
- RadioPlayerCoordinator (updateUserDefaultsLanguage after saveCombined ~860; other resume/switch paths).
- SharedPlayerManager+NowPlaying.swift (~149).
- ViewController (post-clear, hasActive re-detect ~493, 1446).
- Widget providers themselves for optimistic cases (LutheranRadioWidget.swift ~749, 800; LutheranRadioWidgetControl.swift ~78).

The internal observer in WidgetRefreshManager already demonstrates the parallel non-forcing path. Deduplication, coalescing, regress guards, and privacy gating all live inside `refreshIfNeeded`, so duplicate triggers are harmless today.

Late-stage opportunity: Once the event path has demonstrated complete coverage and multiple independent consumers (UI, refresh, LA), some of the direct calls inside main-app mutation paths could be removed or turned into no-ops while keeping the public surface. The observer would become the sole driver for timeline reloads.

**3. Widget / Control Provider state resolution (repeated persistence refresh + snapshot reads)**

Every timeline entry and control value query in widget extension code performs:

```swift
await manager.refreshVisualStateFromPersistence()
if let combined = SharedPlayerManager.loadPersistedWidgetState() { ... }
```

(See: LutheranRadioWidget.swift:getPendingOrCurrentState:307 and Provider paths; LutheranRadioWidgetControl.swift:effectiveVisualStateAndStation:174.)

This is WidgetKit-driven (not a CPU poll loop), but it is an explicit "force read from disk" pattern on every system snapshot request.

Future direction (very late): Rely more heavily on main-app-driven `WidgetCenter.reloadTimelines` (already the dominant mechanism) so providers see fresh snapshots without the refresh call on every execution. The read-refresh guard will likely stay for safety.

**4. Fallback / lifecycle timers that duplicate or guard event-driven surfaces**

- ViewController.setupWidgetActionPolling() (ViewController.swift:589): 30 s repeating `Timer` that calls `checkForPendingWidgetActions()`. Complements the primary Darwin notification listener (`setupDarwinNotificationListener`) and `setupFastWidgetActionChecking`. Widget actions are also delivered via URL schemes and the coordinator.
- ViewController.setupConnectivityCheckTimer() (ViewController.swift:960): 5 s repeating connectivity probe (unrelated to player visual state but touches recovery paths).
- RadioLiveActivityManager.updateTimer (RadioLiveActivityManager.swift): 10–30 s fallback heartbeat. Explicitly demoted in production docs and tests ("NoFallbackTimerStartedByDefault", "timer demoted"). Primary updates come from `contentUpdates` attribute events + `PlayerEvent` routed through `WidgetEventObserver`.
- DirectStreamingPlayer timers (certificateValidationTimer 10 min, bufferingTimer, etc.): Internal to the streaming engine; they drive `saveCurrentState()` which in turn emits events. Not candidates for removal.

The sleep-timer countdown (`SharedPlayerManager+SleepTimer.swift`) uses an internal `Task.sleep` loop + `SleepTimerNotification` (NotificationCenter) precisely "to avoid polling the actor every second". It already produces `playbackIntentChanged` and `visualStateDidChange` events via the normal surfaces.

**5. Coordinator and UIKit-layer direct state synchronization**

- RadioPlayerCoordinator and ViewController contain many eager reads of `currentVisualState`, `currentPlaybackIntent`, `loadPersistedWidgetState`, followed by `updateUI`, `refreshIfNeeded`, `saveCurrentState`, or "force" visual assignments (comments and code around language switch, cold launch guards, post-clear, resume, clearAllLocalState).
- `PlayerViewModel` is still populated primarily by push from the coordinator and DirectStreamingPlayer callbacks rather than pulled reactively from the replaying event stream.
- RadioPlayerView hosts the additive `PlayerEventSubscriber` (via `.task` + `.onChange`) strictly for UI-only side effects while the primary visuals continue to flow through `@Bindable viewModel`.

Late-stage direction: Selected orchestration paths inside the coordinator could migrate to observing `makeEventsStreamWithReplay()` (or `currentState` + specific events) for coordination decisions, leaving direct mutation only for the actual engine surfaces. The push model for PlayerViewModel would remain compatible.

**6. Surfaces that are already event-driven or deliberately non-candidates**

- WidgetRefreshManager and RadioPlayerView (PlayerEventSubscriber) already consume the replaying stream.
- RadioLiveActivityManager consumes Live Activity attribute `contentUpdates` via `WidgetEventObserver` (plus indirect PlayerEvent effects).
- All Tier 1 emissions inside SharedPlayerManager fire after the corresponding state mutation.
- DirectStreamingPlayer remains the owner of actual AV playback; it correctly calls `saveCurrentState()` after mutations (events are emitted on the SharedPlayerManager side).
- Core security actors are deliberately excluded.

**Cross-process and extension reality**

Widget extension processes (home widget, Control Center widget, Live Activity UI) cannot observe `SharedPlayerManager.events` because emission is guarded to the main app process. The architecture therefore keeps snapshot reads (`loadPersistedWidgetState`) + main-app-driven `reloadTimelines` / attribute updates as the cross-process mechanism. Any future consolidation must preserve instant optimistic writes from widget intents and the read-refresh hygiene for long-lived extension processes.

**Selection criteria for actual replacements (when Tier 4 work begins)**

- Only after the event path has multiple independent, long-lived consumers that have been exercised on device for weeks.
- Changes are tiny, isolated, and additive: an imperative call site may be deleted only after the equivalent event trigger is proven to produce identical observable behavior (including timing, coalescing, and privacy gates).
- Legacy forcing surfaces that exist for cross-process instant feedback or process-lifecycle sentinels are documented as permanent compatibility shims; they are not removal targets.
- All edits must upgrade documentation, add SeeAlso links to this roadmap, preserve test seams, and pass the full build + test gates.

- SeeAlso: `WidgetRefreshManager.handlePlayerEvent`, `PlayerEventSubscriber`, `WidgetEventObserver`, `SharedPlayerManager.emit`, `forcePersistVisualState` documentation, `docs/Widget-Presentation-Dataflow.md`, CODING_AGENT.md (Single Source of Truth Principles + event-driven direction).

**Rule (reaffirmed):** Nothing is removed or made secondary until the event-driven path has proven itself as a reliable, complete, and observable replacement for the specific call site under consideration. Direct state access and snapshot writes remain available forever for compatibility and simplicity.

### Tier 5 – Documentation & Tests
- [ ] Expand `Core/Core.docc/Articles/Architecture.md` (or create a dedicated Event-Driven Architecture article) describing the `PlayerEvent` + `SharedPlayerManager.events` model.
- [x] Initial unit test coverage added: `SharedPlayerManagerEventTests.swift` with replay contract test (`makeEventsStreamWithReplay` + `currentState` synthesis) plus live emission forwarding verification (subsequent `streamDidStop` observed on a replaying stream). First micro-step for the test backlog item. Full production-level file header, structured docs, SeeAlso to roadmap / PlayerEvent surfaces, and strict-concurrency-safe collection helper. (2026-07-06)
- [x] Add consumer white-box coverage for `WidgetRefreshManager.handlePlayerEvent` visual derivation (carried vs persisted fallback, `hasError` propagation). **Complete (2026-07-07):** `WidgetRefreshManagerEventTests.swift` exercises `deriveRefreshParameters(for:)` via DEBUG seams without WidgetCenter IPC; bypass seam records identical derivations to the direct white-box helper.
- [x] Add consumer unit coverage for `PlayerEventSubscriber` replay observation (`eventCount`, `lastObservedIntent`, cancel semantics). **Complete (2026-07-07):** `PlayerEventSubscriberEventTests.swift` drives `beginObserving()` for replay-prefix alignment with `currentState`, `_test_applyPlayerEvent(_:)` for intent/metadata observable rules, and `cancel()` isolation; `WidgetRefreshManager` DEBUG observation-suppression seams avoid `AsyncStream` iterator contention without WidgetCenter IPC.
- [x] Cold-launch stale `.thermalPaused` snapshot must not block auto-play when the device has cooled. **Complete (2026-07-07):** `sanitizedVisualStateForCrossProcessRestore`, `visualStateForPersistenceWrite`, `ViewController` post-tuning re-fetch, `testStalePersistedThermalPausedSanitizedToPrePlayOnRestore`. Broader restore semantics remain open under **OI-1**.
- [x] Add comprehensive coverage for every `PlayerEvent` case (emission order, actor isolation, additional late-subscriber scenarios). **Complete:** fourth micro-step finalizes replay prefix + `stop()` order; fifth micro-step adds `markPlaybackStoppedByStreamFailure` order + intent-preservation contract; sixth micro-step adds canonical `setUserPaused()` order; seventh micro-step adds metadata clear (`metadataDidUpdate(nil)`); eighth micro-step adds canonical `setPlaying()` order (recovery path); ninth micro-step adds full language-switch paths (active reset→resume + paused metadata clear); tenth micro-step adds error/recovery late-subscriber replay prefix (`testReplayPrefixAfterStreamFailurePreservesIntentThenReflectsRecovery`); eleventh micro-step adds explicit-pause vs stream-failure late-subscriber contrast (`testReplayPrefixDistinguishesExplicitPauseFromStreamFailure`); twelfth micro-step adds permanent-error `hasError` in replay snapshot (`testReplayPrefixReflectsPermanentErrorInCurrentStateSnapshot`); thirteenth micro-step adds multi-subscriber attach ordering (`testMultiSubscriberReplayAttachOrderingPreservesIndependentPrefixesAndLiveOrder`); fourteenth micro-step adds canonical `markAsUserPaused()` order (`testMarkAsUserPausedEmissionOrderMatchesCanonicalMutationSequence`, mirrors `setUserPaused()` subsequence); fifteenth micro-step adds `streamDidFail(.securityFailure)` classified payload (`testSecurityFailureStreamDidFailPayloadIsFaithfullyEmitted`).
  - Second focused Tier 5 test micro-step (2026-07-06): added `testLiveEmitsTransitionEventsForStopPauseFailAndIntent`. Exercises live `events` stream (via parallel collector) + reliable DEBUG notification seam posted from `emit`. Uses forced transitions, single-batch collection for the stream, and `waitForEmission` (XCTest expectation on the notification) for deterministic assertions. Added test seam in `emit` (DEBUG only). Pre-warm in setUp, 10s timeouts. Production docs + cross-links. Build gate green. The notification seam makes the test independent of AsyncStream iterator timing in the full-app test host while still exercising the production live stream path.
  - Third focused Tier 5 test micro-step (2026-07-06): extended `testLiveEmitsTransitionEventsForStopPauseFailAndIntent` (and setUp) with live coverage for non-nil `.metadataDidUpdate` (via `didUpdateStreamMetadata`) and `.persistedWidgetStateDidUpdate` (via `saveCurrentState` after re-enabling the `hasActiveLutheranWidgets` gate post-`clearAllLocalState`). Updated structured docs, SeeAlso, and collector limits. `streamDidStart` remains intentionally short-circuited under UITestMode (state covered by replay + `currentState`). All prior cases + these two now exercised on the live path.
  - Live Activity / widget test acceleration (same micro-step, 2026-07-06): previously "disabled altogether" (left gate false) to avoid 5-minute stalls caused by `clearAllLocalState` → real `endActivity` IPCs and WidgetCenter queries when LAs/widgets were present. Now the expensive surfaces are accelerated: cheap pre-clear sanitization (nil currentActivity + cancel obs) in the event test setUp, plus UITestMode/isRunningUnderTest guards added to `RadioLiveActivityManager.endActivity`, `WidgetRefreshManager.refreshHasActiveWidgets`, and `performRefresh`. Tests exercise the real persisted write + emit path (gate forced true via seam) with no system service cost. LA test pattern reused. Build green; full coverage without the prior workaround. Roadmap + test docs updated.
  - Fourth focused Tier 5 test micro-step (2026-07-07, partial): added `testReplayStreamPrefixesStateThenForwardsLiveStopEmissionsInOrder` plus test helpers `collectSeamEvents`, `assertEvents(containInOrder:)`, and hardened `collectEvents` / `waitForEvent` subscribe-before-action patterns. Production: `makeEventsStreamWithReplay()` materializes `events` while actor-isolated and yields before return so forwarding iterators attach before live mutations. **Contracts protected:** (1) replay stream delivers exactly four state-prefix events synthesized from `currentState` before live forwarding; (2) canonical `stop()` emission order via ordered subsequence on the DEBUG notification seam (`visualStateDidChange(.userPaused)` → `playbackIntentChanged(.userPaused)` → `streamDidStop`, plus `.persistedWidgetStateDidUpdate` when the write path runs). **Best-effort only:** replay live-forwarding after the prefix (second iterator on the same replay stream during `stop()`); XCTest host AsyncStream attach races can still drop forwarded yields, so this check does not gate the primary contracts. **Remaining open:** emission order for `pause`, `fail`, `start`, and other transitions; additional late-subscriber scenarios; exhaustive replay-forwarding reliability under the test host.
- [ ] Update `README.md` Single Sources of Truth section to include the event emission responsibility of `SharedPlayerManager`.

---

## Selecting and Implementing Micro-Steps

The next item is always the highest-priority remaining entry in the backlog (Tier 3 onward now that Tier 1 and Tier 2 coverage are complete).

Each micro-step is a tiny, isolated, additive change (for example, adding a single emission site or a small non-forcing consumer) that follows every rule in `CODING_AGENT.md`, including reading target files first, using production-level comments, updating cross-references, and passing build gates.

After a change is complete and reviewed, the "Completed" section of this document is updated, the finished item is moved out of the backlog, and priorities are adjusted if new insights emerged.

An item is marked complete only when the emission (or consumption) is wired and the code compiles cleanly with zero warnings.

## Usage of This Document

This file is the single source of truth for the current state of the event-driven refactor.

Contributors address a specific backlog item when making changes. When a backlog item is split, re-prioritized, or when new `PlayerEvent` cases are required, this document is updated in the same change.

The target architecture is one in which the primary way components learn about player state changes is by observing `PlayerEvent`s, while direct state access remains available for compatibility and simplicity.

## Update Log

This document must be maintained after every significant micro-step so that the backlog, completed items, and priorities remain accurate for developers and future agents.

Keep a short chronological log of major milestones:

- `PlayerEvent` vocabulary introduced + `SharedPlayerManager` became authoritative emitter with clean `AsyncStream` (commit 085311d...).
- Tier 1 Emission Coverage cleaned up and completed (emissions added after state mutations inside `SharedPlayerManager` using existing surfaces only; no new record APIs or emission logic in `DirectStreamingPlayer`). Stream*, metadata, visualStateDidChange, and persistedWidgetStateDidUpdate now emitted. Stale comments/docs cleaned. (2026-07-03)
- Surface cleanup + docs uplift (commit d219f8e): improved docs in `SharedPlayerManager.swift` + `PlayerViewModel.swift` promoting event-driven non-forcing architecture, scoping legacy forcing shims to widget optimistic paths, and strengthening SeeAlso/cross-links to `PlayerEvent` / ``events`` / roadmap. (2026-07-03)
- Tier 2 first consumer complete: `WidgetRefreshManager` now contains a lightweight internal `PlayerEvent` observer (additive only). All changes follow production documentation standards (structured `///`, file headers, AGENT NOTE, present-tense final architecture language, cross-links to `events`, `PlayerEvent`, roadmap, Architecture.md, CODING_AGENT.md). Existing snapshot + refresh paths untouched. Build + test gates passed. (2026-07-04)
- Live Activity attribute events complete: `RadioLiveActivityManager` now observes the `activity.contentUpdates` AsyncStream (events for ContentState + state). Aligns dedup record + self-heals on dismissal. Additive, production docs, cross-links. (2026-07-04)
- Tier 2 polish (consolidation): The common observation pattern used by both `WidgetRefreshManager` (PlayerEvent) and `RadioLiveActivityManager` (Live Activity attribute events) has been extracted into the internal `WidgetEventObserver` helper. Both managers delegate to it while preserving all task seams, property names, contracts, and behavior. `WidgetEventObserver` is now part of the cross-target shared sources, carries full production documentation, and is referenced from the dataflow and roadmap docs. No behavior change, no new polling. (2026-07-04)
- UI subscriber (main app layer): `PlayerEventSubscriber` added to `RadioPlayerView` (the primary player UI hosting view). Uses `@Observable`, `@State`, `.task`, `.onChange`, and `Task` for lightweight reactions to `playbackIntentChanged` + state transition events. Value-type updates; UI-only side effects only; zero impact on existing bindings or Core. Full structured docs and header uplift. Roadmap updated. (2026-07-05)
- Tier 3 current-state replay delivered: `PlayerCurrentState` type added to the canonical vocabulary in `PlayerVisualState.swift`. `SharedPlayerManager` now provides `currentState` (snapshot for initialization) and `makeEventsStreamWithReplay()` (replaying stream). `PlayerEventSubscriber` consumes the replaying stream. All touched files received production-level documentation (structured `///`, present-tense final language, SeeAlso links to new surfaces and this roadmap, AGENT NOTE where appropriate). Changes are strictly additive. No existing `events` contract, imperative paths, or snapshot surfaces were altered. (2026-07-06)
- Tier 3 architectural evaluation complete: per-actor `AsyncStream`s are sufficient. Higher-level typed event bus rejected. Decision, rationale, and present-tense status recorded in Completed section and Architecture Status. `WidgetEventObserver` is the uniform consumption abstraction. Roadmap updated with final-architecture language. (2026-07-06)
- Error and recovery surface finalized: evaluation of `streamDidFail` classification, recovery paths, replay, and consumers confirms the existing `PlayerEvent` cases plus `PlayerCurrentState.hasError` constitute the complete observation contract. Documentation in `PlayerVisualState.swift`, `WidgetRefreshManager.swift`, and this roadmap updated to production language. The item is complete; no new cases added. (2026-07-06)
- Tier 3 post-delivery polish (non-behavioral): added three zero-cost convenience computed properties to `PlayerCurrentState` (`isActivelyPlaying`, `isBlockedByStickyIntent`, `isInPermanentError`) with production documentation; strengthened `makeEventsStreamWithReplay` and `currentState` docs with explicit statement of the replay contract (no synthesized `streamDid*` verbs; state expressed via snapshot fields); added targeted SeeAlso links to `PlaybackIntent.isStickyPauseOrLock`, the new properties, and the error/recovery evaluation. Inline comments tightened in replay synthesis site. All changes strictly additive and documentation-only. Roadmap Update Log and cross-links updated in the same micro-step. Files left in a strictly better state per CODING_AGENT.md standards. (2026-07-06)
- Tier 4 identification complete: exhaustive location of all direct "force" calls (forcePersistVisualState, _forceSet..., forceStale..., refreshVisualStateFromPersistence), imperative refreshIfNeeded sites, widget-provider persistence refreshes, fallback timers (ViewController widget-action polling + connectivity, LA updateTimer), and coordinator/VC eager state sync patterns. Full categorized inventory recorded under Tier 4 with production language, call-site references, late-stage caveats, and cross-process realities. The "Identify..." backlog item is marked complete. No code behavior changed; only documentation and analysis. (2026-07-06)
- First focused Tier 5 test micro-step complete: added `Lutheran RadioTests/SharedPlayerManagerEventTests.swift`. Contains the replay contract test exercising `PlayerCurrentState`, `makeEventsStreamWithReplay()`, and live event forwarding (a `streamDidStop` emitted after an explicit `stop()` is observed by the subscriber). All changes additive, use structured documentation, cross-link the roadmap and `PlayerEvent`, follow strict concurrency, and pass build + test gates on the scheme. The test file and helper were iterated to production quality. Roadmap and Update Log updated in the same change. (2026-07-06)
- Live emission test iteration (same micro-step): switched to single-batch collection on the live stream + added reliable DEBUG notification seam posted from `emit`. `waitForEmission` uses XCTest expectation on the seam for deterministic results. The test now exercises both the production `AsyncStream` and proves every emit site is hit. Build remains green. (2026-07-06)
- Second focused Tier 5 test micro-step: added live emission test `testLiveEmitsTransitionEventsForStopPauseFailAndIntent` in `SharedPlayerManagerEventTests.swift`. Covers direct observation of `playbackIntentChanged`, `visualStateDidChange(.userPaused)`, `streamDidStop`, `streamDidPause`, and `streamDidFail(.transientFailure)` (and other classifications) on the live `events` AsyncStream using public/internal surfaces. Full structured `///` documentation, SeeAlso cross-links to `PlayerEvent` / `emit` / roadmap / CODING_AGENT.md, and present-tense final-architecture language. Roadmap updated with sub-progress note under Tier 5. No production behavior changed; strictly additive test coverage. (2026-07-06)
- Third focused Tier 5 test micro-step: live coverage for `.metadataDidUpdate(non-nil)` (via `didUpdateStreamMetadata("Test Program • Speaker")`) and `.persistedWidgetStateDidUpdate` (via `saveCurrentState()`). Added gate re-enable in setUp after `clearAllLocalState` (which forces false) so the privacy-guarded write path emits under test. Updated test docs, collector limit, SeeAlso, and this roadmap (Completed list, Tier 5 detail, Update Log). All changes strictly additive; no production paths, contracts, or security surfaces touched. (2026-07-06)
- Live Activity / widget test acceleration for Tier 5 coverage: hardened `endActivity`, `refreshHasActiveWidgets`, and related paths with UITestMode guards + cheap sanitization in `SharedPlayerManagerEventTests`. Allows the persisted gate to be enabled for full live emission coverage without the previous 5-minute system service stalls. (2026-07-06)
  - Fourth focused Tier 5 test micro-step (partial, 2026-07-07): `testReplayStreamPrefixesStateThenForwardsLiveStopEmissionsInOrder` protects the Tier 3 replay prefix contract and canonical `stop()` emission order (hybrid collection: replay stream for prefix, DEBUG notification seam for order). `makeEventsStreamWithReplay()` now materializes the live stream while actor-isolated and yields before return. Test helpers `collectSeamEvents`, `assertEvents(containInOrder:)`, and subscribe-before-action hardening in `collectEvents` / `waitForEvent`. Replay live-forwarding assertion is best-effort only. Comprehensive emission-order and late-subscriber coverage remains open. Build gate green in known-good state.
  - Fifth focused Tier 5 test micro-step (2026-07-07): `testStreamFailureEmissionOrderPreservesIntentAndMutationSequence` protects the `markPlaybackStoppedByStreamFailure(_:)` contract — ordered subsequence `visualStateDidChange(.userPaused)` → `streamDidFail(…)` → `.persistedWidgetStateDidUpdate`, negative guards excluding `playbackIntentChanged` / `streamDidPause` / `streamDidStop`, and postcondition that `currentPlaybackIntent` is unchanged (auto-resume semantics). Collection via DEBUG notification seam. Remaining open: pause/start order, metadata clears, additional late-subscriber scenarios.
  - Sixth focused Tier 5 test micro-step (2026-07-07): `testSetUserPausedEmissionOrderMatchesCanonicalMutationSequence` protects the canonical `setUserPaused()` contract — ordered subsequence `visualStateDidChange(.userPaused)` → `playbackIntentChanged(.userPaused)` → `streamDidPause` → `.persistedWidgetStateDidUpdate`, negative guards excluding `streamDidStop` / `streamDidFail`, and sticky `.userPaused` postconditions on visual + intent. Collection via DEBUG notification seam. Remaining open: start order, metadata clears, additional late-subscriber scenarios.
  - Seventh focused Tier 5 test micro-step (2026-07-07): `testMetadataClearEmitsNilWithoutPlaybackMutation` protects the `clearSoftPauseMetadataStashForLanguageChange()` contract — exactly one `.metadataDidUpdate(nil)` emission, negative guards excluding stream/visual/intent/persist events, nil `currentState.streamMetadata` postcondition, and unchanged visual + intent. Collection via DEBUG notification seam. Remaining open: `streamDidStart` order (UITestMode short-circuit), full language-switch paths, additional late-subscriber scenarios.
  - Eighth focused Tier 5 test micro-step (2026-07-07): `testSetPlayingEmissionOrderMatchesCanonicalMutationSequence` protects the `setPlaying()` contract on the recovery path from `.userPaused` — ordered subsequence `visualStateDidChange(.playing)` → `playbackIntentChanged(.shouldBePlaying)` → `streamDidStart` → `.persistedWidgetStateDidUpdate`, negative guards excluding pause/stop/fail verbs, and `.playing` / `.shouldBePlaying` postconditions. Production: `setPlaying()` UITestMode path now performs canonical mutations + emissions (skips Live Activity / Now Playing only). Collection via DEBUG notification seam. Remaining open: full language-switch paths, additional late-subscriber scenarios.
  - Ninth focused Tier 5 test micro-step (2026-07-07): `testActiveLanguageSwitchResetThenResumeEmissionOrderPreservesIntent` and `testPausedLanguageSwitchFullPathClearsMetadataWithoutVisualOrIntentMutation` protect the full language-switch paths — active-intent (`resetToPrePlayForNewStream` → `switchToStream` → `setPlaying`: prePlay→playing subsequence, intent preserved, no terminal stream verbs) and paused (`switchToStream` + `clearSoftPauseMetadataStashForLanguageChange`: metadata nil, visual/intent unchanged). Collection via DEBUG notification seam. Remaining open: additional late-subscriber scenarios.
  - Tenth focused Tier 5 test micro-step (2026-07-07): `testReplayPrefixAfterStreamFailurePreservesIntentThenReflectsRecovery` protects the Tier 3 error/recovery late-subscriber contract — replay prefix after transient failure carries `.userPaused` visual with preserved `.shouldBePlaying` intent (`isBlockedByStickyIntent == false`), fresh replay prefix after `setPlaying()` reflects recovered `.playing` state, best-effort recovery forwarding on the first replay stream. Remaining open: explicit-pause vs failure contrast, permanent-error `hasError` in prefix, multi-subscriber ordering.
  - Eleventh focused Tier 5 test micro-step (2026-07-07): `testReplayPrefixDistinguishesExplicitPauseFromStreamFailure` protects the explicit-pause vs stream-failure late-subscriber contrast — identical grey `.userPaused` replay visual with divergent `.playbackIntentChanged` values (sticky `.userPaused` vs preserved `.shouldBePlaying`) and matching `isBlockedByStickyIntent` postconditions across both phases in one isolated test. Remaining open: permanent-error `hasError` in replay snapshot, multi-subscriber ordering.
  - Twelfth focused Tier 5 test micro-step (2026-07-07): `testReplayPrefixReflectsPermanentErrorInCurrentStateSnapshot` protects the permanent-error `hasError` late-subscriber contract — security lock (`.securityLocked` + `hasError` / `isInPermanentError`), permanent stream failure (grey `.userPaused` + `hasError` when engine `hasPermanentError` is set, persisted snapshot `hasError`), and transient-failure contrast (same grey visual, `hasError == false`). Remaining open: multi-subscriber attach ordering.
  - Thirteenth focused Tier 5 test micro-step (2026-07-07): `testMultiSubscriberReplayAttachOrderingPreservesIndependentPrefixesAndLiveOrder` protects multi-subscriber replay attach ordering — prefix independence (early vs late subscriber, no historical `streamDid*` synthesis) and identical prefixes for same-state concurrent streams via `collectEventsConcurrently`. Tier 5 comprehensive emission-order and late-subscriber coverage is complete.
  - Fourteenth focused Tier 5 test micro-step (2026-07-07): `testMarkAsUserPausedEmissionOrderMatchesCanonicalMutationSequence` protects the `markAsUserPaused()` contract — ordered subsequence `visualStateDidChange(.userPaused)` → `playbackIntentChanged(.userPaused)` → `streamDidPause` → `.persistedWidgetStateDidUpdate` (mirrors `setUserPaused()`), negative guards excluding `streamDidStop` / `streamDidFail`, and sticky `.userPaused` postconditions. Collection via DEBUG notification seam. `markAsUserPaused()` production `///` docs strengthened with emission-order Note and SeeAlso cross-links.
  - Fifteenth focused Tier 5 test micro-step (2026-07-07): `testSecurityFailureStreamDidFailPayloadIsFaithfullyEmitted` protects the `streamDidFail(.securityFailure)` classified payload contract — ordered subsequence `visualStateDidChange(.userPaused)` → `streamDidFail(.securityFailure)` → `.persistedWidgetStateDidUpdate`, exactly one fail emission with `.securityFailure` (no transient/permanent misclassification), negative guards excluding `playbackIntentChanged` / `streamDidPause` / `streamDidStop`, and unchanged `currentPlaybackIntent`. Collection via DEBUG notification seam. Closes the `StreamErrorType` classification gap for the security branch on the emitter path.
  - Sixteenth focused Tier 5 consumer micro-step (2026-07-07): `WidgetRefreshManagerEventTests.swift` protects the Tier 2 `handlePlayerEvent` derivation contract — carried `.visualStateDidChange` preference, persisted fallback for `.streamDidStart` / `.persistedWidgetStateDidUpdate`, `.prePlay` default without snapshot, `hasError` from `loadSharedState`, and bypass-seam parity with `_test_deriveRefreshParameters`. Production: `deriveRefreshParameters(for:)` extracted; DEBUG seams compiled out of Release.
  - Seventeenth focused Tier 5 consumer micro-step (2026-07-07): `PlayerEventSubscriberEventTests.swift` protects the Tier 2 UI subscriber contract — `beginObserving()` replay prefix (`eventCount == 4`, `lastObservedIntent` aligned with `currentState.playbackIntent`), `_test_applyPlayerEvent(_:)` intent/metadata observable rules, and `cancel()` replay-stream isolation. Production: `PlayerEventSubscriber` DEBUG apply seam + SeeAlso cross-link; `WidgetRefreshManager` DEBUG `_test_setSuppressPlayerEventObservation` / `_test_suspendPlayerEventObservation` for iterator isolation in XCTest.
- Cold-launch thermal snapshot sanitization (2026-07-07, adjacent SSOT fix): stale persisted `.thermalPaused` no longer blocks cold-launch auto-play when `ProcessInfo.thermalState` is nominal/fair. Read sanitization + write suppression in `SharedPlayerManager`; post-tuning visual re-fetch in `ViewController`; `testStalePersistedThermalPausedSanitizedToPrePlayOnRestore`. Open Issue **OI-1** filed for the broader verbatim `PersistedWidgetState` restore problem (development iPhone unexpected cold start on every power-on).
- MediaRemoteUI launch watchdog fix (2026-07-08): async two-phase ``teardownNowPlayingSession()`` + `WidgetRefreshManager.isSessionTeardownInProgress` gate. See Completed section. `// SAFETY:` on detached phase 2 documents watchdog rationale. Build + test gates required.
- Session + widget teardown follow-up (2026-07-08): ``performSessionAndWidgetTeardown()`` + sync termination helper + post-stop widget hygiene. See Completed section. DEBUG lock/unlock cycle logs on `SceneDelegate` + `[SessionTeardown]`. Build + test gates required.

---

This document is the single source of truth for the current state of the event-driven architecture. It is consulted at the beginning of any work on this area.
